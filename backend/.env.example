# Backend (DigitalOcean App Platform)
NODE_ENV=production
PORT=5100

# Postgres (Prisma)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public

# Auth
JWT_SECRET=REPLACE_ME

# CORS
CORS_ORIGINS=*

# Queue mode
GRADING_QUEUE_MODE=cloudflare

# Cloudflare API (used by backend to publish queue messages)
CF_ACCOUNT_ID=REPLACE_ME
CF_API_TOKEN=REPLACE_ME
CF_QUEUE_ID=grading-fast
CF_API_BASE_URL=https://api.cloudflare.com/client/v4

# Shared secret for Cloudflare Worker -> backend internal endpoints
GRADING_WORKER_TOKEN=REPLACE_ME

# DO pull-worker is disabled when Cloudflare Worker is the queue consumer
GRADING_PULL_WORKER_ENABLED=false

# Dispatch/recovery tuning
GRADING_DISPATCH_LOOP_INTERVAL_MS=5000
GRADING_HEARTBEAT_INTERVAL_MS=60000
GRADING_STALE_PROCESSING_MS=1200000
GRADING_FAST_MAX_PAGES=4

# Object storage (new uploads should be R2)
OBJECT_STORAGE_PROVIDER=r2
R2_ACCOUNT_ID=REPLACE_ME
R2_ACCESS_KEY_ID=REPLACE_ME
R2_SECRET_ACCESS_KEY=REPLACE_ME
R2_BUCKET_NAME=worksheet-images

# Optional (R2 S3-compatible endpoint override)
# R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com

# Optional (if you serve images publicly)
# R2_PUBLIC_BASE_URL=https://pub-xxxxx.r2.dev

# Legacy S3 (keep for old images)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=worksheet-images

# Legacy python grader (not used when Cloudflare Worker grades)
PYTHON_API_URL=https://REPLACE_ME
