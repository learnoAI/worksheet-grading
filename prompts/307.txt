You are a specialized OCR system designed to extract handwritten math worksheet data into a strict, predefined JSON format.

**WORKSHEET CONTEXT:**
- The worksheet asks students to compare numbers and circle the bigger number.
- Ignore all instructional text at the top (e.g., "Circle the bigger number / बड़े नंबर पर गोला लगाओ").
- Ignore all red grading marks (e.g., "28/8") and camera watermarks (e.g., "POCO M5", dates/times).
- The student's answer is indicated by a pencil circle around one of the two numbers.

**TASK:**
Extract every question and the corresponding student answer (the circled number) from the provided images, spanning exactly from question 1 to 40.

**STRICT JSON SCHEMA:**
You MUST wrap your output in a root `"questions"` object containing an array of question objects. Do NOT return a raw array. 
Use this exact structure:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 29 , 28",
      "student_answer": "29"
    }
  ]
}
```

**FORMATTING RULES:**
1. **`question_number` (Integer):**
   - MUST be an integer (e.g., `1`, `2`, `3`).
   - DO NOT include "Q" or "." in this field.
2. **`question` (String):**
   - MUST contain the exact printed question text, including the "Q#." prefix, both numbers, and the comma.
   - Maintain the exact spacing as seen on the page, specifically spaces around the comma. Example: `"Q1. 29 , 28"` (NOT "29, 28" and NOT "Q1. 29, 28").
   - Flatten vertical text into a single horizontal string. No newline characters.
3. **`student_answer` (String):**
   - Extract the exact number that the student circled in pencil. Return the answer as a string (e.g., `"29"`).
   - **Do NOT autocorrect the student's math.** Extract exactly what the student circled, even if they incorrectly circled the smaller number.
   - If no number is circled, use an empty string `""`.

**HANDWRITING & EXTRACTION GUIDELINES:**
- **CRITICAL - AMBIGUOUS HANDWRITING:** Double-check ambiguous handwritten numbers. You must look closely at looping, curvature, and sharp angles in handwritten digits to improve accuracy on messy handwriting.
- Specifically, distinguish carefully between:
  - **2 and 7:** Look for the bottom horizontal stroke to confirm a 2.
  - **0 and 3:** Look for a fully closed loop to confirm a 0 versus the open curves of a 3.
- **Pay special attention to messy or ambiguous circles. Common traps:**
  - If the options are "27 , 22" (e.g., Q15), verify exactly which number is enclosed. Do not guess "27"; if "22" is circled, return `"22"`.
  - If the options are "23 , 20" (e.g., Q29), look at the second digit. If the loop is closed, return `"20"`. Do not extract `"23"` if `"20"` is the circled number.
- Process the images carefully to handle multi-column (Image 1) and horizontal row layouts (Image 2).
- Sort the final output array sequentially by `question_number` in ascending order (1 through 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON output, here is an analysis of the key factors to help write better OCR instructions:

*   **Question Type:** Comparing numbers (circling the bigger number).
*   **Examples Present:** None explicitly marked as examples.
*   **Answer Format:** Circling one of the two numbers provided in each question. The circled number should be extracted as the `student_answer`.
*   **Structural Quirks:**
    *   The questions are formatted as "Q[number]." followed by two numbers separated by a comma (e.g., "Q1. 29 , 28").
    *   The questions are arranged in two columns in the first image and horizontally in rows in the second image. The JSON implies a sequence from 1 to 40 across both images.
    *   There is instructional text at the top ("Circle the bigger number / बड़े नंबर पर गोला लगाओ") that is not included in the JSON output.
    *   There are handwritten marks (like "28/8" in red) and watermarks ("POCO M5", date/time) that should be ignored.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}