You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every subtraction question and its corresponding student answer from the image provided.

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a valid JSON object with a single root key `"q_and_a"`, which contains an array of objects.
Use EXACTLY this structure:
```json
{
  "q_and_a": [
    {
      "question_number": "Q1.",
      "question": "849 - 42",
      "student_answer": "807"
    }
  ]
}
```

**STRICT EXTRACTION & FORMATTING RULES:**

1. **`question_number` Field:**
   - MUST be formatted as a string including the 'Q' and the period (e.g., `"Q1."`, `"Q2."`).
   - DO NOT output an integer (e.g., `1` is wrong).

2. **`question` Field:**
   - Extract ONLY the mathematical equation (e.g., `"849 - 42"`).
   - **CRITICAL:** STRICTLY OMIT the question label (e.g., "Q1.", "Q2.") from this field. Do not duplicate the question number here.
   - Flatten the vertical stacked numbers into a single horizontal equation separated by spaces (e.g., top number, minus sign, bottom number).
   - NO newline characters in the string.

3. **`student_answer` Field:**
   - Extract the handwritten number written in pencil below the horizontal line.
   - Return the answer as a string (e.g., `"807"`).
   - If the answer slot is completely blank, use an empty string `""`.

4. **HANDWRITING & TEACHER MARKS EXTRACTION GUIDELINES:**
   - **Ignore Teacher Marks:** The teacher has marked answers with red ink (ticks or circles). DO NOT confuse these red marks with digits (e.g., a tick is not a "1" or a "7").
   - **Digit Precision:** Pay extreme attention to distinguishing similar looking digits:
     - `1` vs `7`
     - `3` vs `7`
     - `6` vs `0`
     - `8` vs `9`
   - *Hint based on prior errors:* Carefully check the endings of numbers to ensure "1"s are not misread as "7"s, and "4"s or "3"s are correctly identified.

5. **PROCESSING ORDER:**
   - The problems are arranged in a grid with vertical columns. Follow the numbering to ensure you extract all questions.
   - Sort the final output array strictly by `question_number` ascending (Q1. to Q40.).

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Subtraction.
*   **Examples Present:** None clearly marked as examples; however, the first few questions appear printed rather than handwritten, suggesting they might serve as a guide.
*   **Answer Format:** Handwritten answers below the line for each subtraction problem.
*   **Structural Quirks:** The problems are arranged in a grid with vertical columns. Each question is numbered sequentially (e.g., Q1., Q2., Q6., etc.). The teacher has marked the handwritten answers with a red pen (ticks or circles).
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}