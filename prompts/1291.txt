You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided image. The image contains math division problems arranged in two vertical columns. The student's answers are handwritten in pencil to the right of the equals sign, and grading marks (red checkmarks) may be drawn over them.

**JSON SCHEMA ENFORCEMENT:**
You MUST output a single JSON object with a root key `"questions"` containing an array of objects. Do not return a raw array.

```json
{
  "questions": [
    {
      "question_number": <integer>,
      "question": "<string>",
      "student_answer": "<string>"
    }
  ]
}
```

**FIELD FORMATTING RULES:**
1. **`question_number` (Integer):**
   - Extract the numeric value strictly as an integer. 
   - Strip away the 'Q', spaces, and punctuation (e.g., if you see "Q 1.", return the integer `1`).
2. **`question` (String):**
   - Must exactly reproduce the text with this precise spacing format: "Q {number}. {problem} =".
   - **CRITICAL SPACING:** You MUST include a space between the "Q" and the number, followed by a period, followed by a space (e.g., `"Q 1. 78 \u00f7 6 ="`). Do not format as "Q1.".
   - Flatten vertical text into a single horizontal line.
   - Use the standard division symbol (`÷` / `\u00f7`).
   - Do NOT include newline characters.
3. **`student_answer` (String):**
   - Extract exactly what the student wrote in pencil in the blank space to the right of the equals sign.
   - Ignore all red checkmarks or grading marks overlapping the text.
   - If the answer slot is blank, return an empty string `""`.
   - Always return this value as a string.

**HANDWRITING EXTRACTION & VERIFICATION GUIDELINES:**
- **Digit Confusion Warning:** Look extremely closely at handwritten digit formation. You must carefully distinguish **4** vs **9**, **1** vs **7**, **9** vs **8**, and **6** vs **0**. 
- **Double-Check Step:** Before finalizing the `student_answer`, mentally double-check your reading of the handwritten digits against common math facts (e.g., 98 ÷ 7 = 14, not 19). A sloppily written '4' is frequently misread as a '9'.
- **Layout Navigation:** Process the worksheet across both vertical columns, tracking the question numbers carefully to ensure no questions are skipped.
- **Sorting:** Sort the final JSON array in strict ascending numerical order by `question_number`.

**1-SHOT EXAMPLE:**
If the image shows "Q 1. 78 ÷ 6 = 13" with a red checkmark over the 13, your output object for that item must be EXACTLY:
```json
{
  "question_number": 1,
  "question": "Q 1. 78 \u00f7 6 =",
  "student_answer": "13"
}
```

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Math problems focusing on division.
*   **Examples Present:** None.
*   **Answer Format:** Students write their answers in the blank space provided to the right of the equals sign.
*   **Structural Quirks:**
    *   The layout uses two vertical columns of questions.
    *   Each question starts with a specific numbering format, e.g., 'Q 1.', 'Q 2.'.
    *   Red checkmarks are drawn over the student's handwritten answers, indicating grading.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}