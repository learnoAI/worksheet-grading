You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every division question and its corresponding student answer from the image provided. Ignore all teacher grading marks (such as large checkmarks, circles, or overall scores at the top).

**JSON SCHEMA ENFORCEMENT:**
You MUST output a single JSON object with a root key `"results"`. This key must contain an array of objects representing each question. Do NOT return the array directly at the root level.
{format_instructions}

**Target Structure Example:**
```json
{
  "results": [
    {
      "question_number": 1,
      "question": "Q1. 15 ÷ 5 =",
      "student_answer": "3"
    },
    {
      "question_number": 26,
      "question": "Q26. 24 ÷ 8 =",
      "student_answer": ""
    }
  ]
}
```

**DO NOT** use dynamic keys.
**KEYS MUST BE:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES:**
1. **question_number (Integer):**
   - The standalone integer ID of the question (e.g., 1, 2, 26).
2. **question (String):**
   - Must start with the specific prefix label exactly as written (e.g., "Q1.", "Q21.").
   - Accurately extract the division symbol (`÷`). Do not confuse it with `+` or `-`.
   - Flatten text into a single horizontal line ending with the equals sign (`=`).
   - **NO** newline characters in the string.
3. **student_answer (String):**
   - Extract exactly the handwritten pencil digits placed immediately to the right of the equals (`=`) sign.
   - If the answer slot is completely blank (e.g., unanswered questions like Q26-Q29), you MUST return an empty string `""`.
   - Return the answer strictly as a string.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at pencil digit formation.
- Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
- If a number is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER & LAYOUT:**
- The worksheet uses a two-column vertical layout. Read and track the question numbers carefully across both columns.
- Sort the final objects inside the `"results"` array strictly by `question_number` in ascending order (1, 2, 3...).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an analysis of the key factors to guide OCR instructions:

*   **Question Type:** Arithmetic division equations.
*   **Examples Present:** None. Every numbered item is a standard question requiring a student answer.
*   **Answer Format:** Handwritten digits placed immediately to the right of the equals (`=`) sign. Unanswered questions (like Q26-Q29) are expected to return an empty string (`""`).
*   **Structural Quirks:**
    *   **Two-Column Layout:** The questions are arranged in two separate vertical columns on each page.
    *   **Specific Numbering:** Question numbers use a specific prefix format: 'Q' followed by the number and a period (e.g., "Q1.", "Q21."). The JSON expects this exact string in the `question` field, but an integer in the `question_number` field.
    *   **Symbol Recognition:** Requires accurate extraction of the division symbol (`÷`), ensuring it is not confused with a plus (`+`) or minus (`-`) sign.
    *   **Grading Marks:** There are large, overlapping teacher grading marks (like the large blue checkmark and scores at the top) that the OCR needs to ignore to accurately capture the underlying text and student answers.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}