You are a literal, pattern-agnostic OCR engine. Your task is to transcribe handwritten text from a math worksheet image into JSON.

**CRITICAL CONTEXT:**
The student who filled out this worksheet **did not solve the math problems correctly**. In many cases (specifically Q1, Q2, Q7, Q11, etc.), **the student simply copied the first number (the minuend) into the answer box**.
*   **Example:** For `100 - 100`, the student wrote `100`.
*   **Your Job:** You must transcribe `100`. If you output `0` because `100-100=0`, you fail.
*   **DO NOT CALCULATE.**

**OUTPUT SCHEMA:**
Return a valid JSON List of Objects. No markdown text outside the JSON.
```json
[
  {
    "question_number": 1,
    "question": "Q1. 100 - 100",
    "student_answer": "100"
  },
  ...
]
```

**EXTRACTION RULES:**

**1. `question_number` (Integer):**
*   Extract the numeric digits only.
*   Example: "Q1." -> `1`, "Q35." -> `35`.

**2. `question` (String):**
*   Format: `"QX. NNN - NNN"`
*   Ensure exactly one space before and after the hyphen.
*   Example: `"Q1. 100 - 100"`

**3. `student_answer` (String):**
*   **Strict Visual Transcription:** Write exactly what is in the answer box.
*   **NO MATH CORRECTION:**
    *   If the question is `100 - 100` and the text looks like `100`, output `"100"`.
    *   Do NOT output `0`, `00`, or blank unless the student actually wrote that.
*   **ANTI-REPETITION (Row Independence):**
    *   Do not look at the previous line to guess the current line.
    *   **Specific Check for Q6:** Visually verify the answer for Question 6. It is **NOT** the same as Question 5. If Q5 is `100` and Q6 looks like `200`, you must write `"200"`.
    *   **Specific Check for Q1 & Q2:** The student wrote the full three digits (`100`, `200`) in the answer slot. Transcribe all three digits.

**FINAL CHECKLIST BEFORE OUTPUT:**
1.  Did I calculate `100 - 100 = 0`? If yes, **STOP** and change it to the visual text `"100"`.
2.  Did I copy the answer from Q5 into Q6? Check Q6 again.
3.  Are my `question_number` values Integers?

**Process every single row (1 through 40).**

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}