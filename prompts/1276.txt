You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet images. 

**JSON SCHEMA ENFORCEMENT:**
You must output exactly and ONLY a JSON object with a single root key `"questions"` containing an array of objects.
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 26 x 47",
      "student_answer": "1222"
    }
  ]
}
```

**DATA TYPES & FORMATTING RULES:**

1. **`question_number` (Integer):**
   - Must be a strict integer type (e.g., `1`, `2`, `10`).
   - Do NOT include the "Q" or the period in this field.

2. **`question` (String):**
   - Must exactly begin with the explicit label (e.g., "Q1. ", "Q2. ").
   - **CRITICAL - EXACT TRANSCRIPTION & ORDERING:** You must flatten the vertical problem into a single horizontal string. 
   - **RULE:** The **TOP** number must come **FIRST**, followed by " x ", followed by the **BOTTOM** number. 
   - **WARNING - DO NOT SWAP OPERANDS:** Do not reorder the numbers based on size or value. If a printed `26` is stacked above `x 47`, it MUST become exactly `"Q1. 26 x 47"`. If you output `"Q1. 47 x 26"`, you have failed.
   - Do NOT include newline characters (`\n`).

3. **`student_answer` (String):**
   - Extract exactly the student's handwritten answer.
   - **IGNORE INTERMEDIATE WORKING:** Students show intermediate partial products. You must completely ignore these middle calculation steps. 
   - Extract **ONLY the final sum** located at the absolute bottom of the handwritten text, strictly beneath the handwritten horizontal line.
   - The final answer must be returned as a string. If the final answer slot is completely blank, use an empty string `""`.

**READING ORDER & MAPPING GUIDELINES (CRITICAL):**
- **Do NOT assume reading order blindly.** The worksheet is a dense grid, and reading across columns vs. down rows can cause severe misalignments.
- **Anchor to the Labels:** You MUST locate the exact printed label ("Q1.", "Q2.", "Q3.", etc.) on the page, and only transcribe the specific math problem physically grouped with that label.
- Track down every question from 1 sequentially up to 40. Ensure the transcribed equation matches the explicit question number printed next to it.
- Ensure the final output array is sorted by `question_number` in ascending order (1, 2, 3... 40).

**HANDWRITING EXTRACTION GUIDELINES:**
- Pay special attention to distinguishing **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**.
- If a number is messy, select the most likely digit based strictly on visual stroke evidence. Do not guess or hallucinate digits.
- Verify that your final string for `question` perfectly maps to the printed numbers for that specific Q-label, and that `student_answer` perfectly maps to the handwritten final sum for that specific problem.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an analysis of the key factors to help write better OCR instructions:

*   **Question Type:** 2-digit by 2-digit multiplication.
*   **Examples Present:** None. There are no pre-solved examples on either page to guide the format.
*   **Answer Format:** Students write their answers underneath the provided horizontal line. Notably, they show their intermediate working (partial products). The OCR instruction must explicitly state to extract *only* the final sum at the bottom of the handwritten text, ignoring the intermediate calculation steps.
*   **Structural Quirks:**
    *   **Vertical to Horizontal Conversion:** The printed worksheet formats the problems vertically (stacked numbers), but the target JSON requires them to be flattened into a horizontal string (e.g., printed vertical `26` over `x 47` becomes `"Q1. 26 x 47"`).
    *   **Grid Layout:** The questions are arranged in a dense, 5-column grid layout, requiring careful spatial parsing to read top-to-bottom within columns, then left-to-right across the page.
    *   **Numbering:** Questions have explicit, sequentially numbered labels prefixed with "Q" (e.g., "Q1.", "Q2.").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}