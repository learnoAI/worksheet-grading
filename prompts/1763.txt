You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every vertical subtraction question and its corresponding student answer written under the line.

**JSON SCHEMA ENFORCEMENT:**
You must output a single JSON object containing a `"questions"` key. The value of `"questions"` must be an array of objects. 
DO NOT output a direct array. DO NOT use dynamic keys.
Format your output exactly like this:
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 450 - 80",
      "student_answer": "370"
    }
  ]
}

**EXTRACTION RULES:**

1. **`question_number` (Integer):**
   - Extract the logical sequential question index as a pure **integer** (e.g., 1, 2, 24).
   - Do NOT include "Q" or "." in this field (e.g., use `1`, not `"Q1."`).

2. **`question` (String):**
   - Flatten the vertical subtraction arrangement into a single horizontal line (e.g., "Q1. 450 - 80").
   - **STRICT TRANSCRIPTION:** Transcribe the question text *exactly* as it appears in the image.
   - **DO NOT AUTO-CORRECT:** If a question label is cut off, partially printed, or malformed (e.g., if you see "24. 450 - 80" instead of "Q24. 450 - 80", or "5. 530 - 60" instead of "Q25. 530 - 60"), output exactly what is visible. Do not add missing 'Q's or fix numbers to fit a pattern.
   - NO newline characters in the string.

3. **`student_answer` (String):**
   - Extract exactly the handwritten number written in pencil below the horizontal line.
   - Return the answer as a string. If the answer slot is blank, use an empty string `""`.
   - Distinguish carefully between messy digits (e.g., 9 vs 8, 6 vs 0, 1 vs 7 vs 4).

**PROCESSING ORDER:**
- Process and output the questions sorted by `question_number` ascending (1 through 40).
- Handle multiple columns correctly by tracking the sequential Question IDs.
- Return ONLY valid JSON, with no markdown formatting outside of the JSON block.

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Subtraction.
*   **Examples Present:** None.
*   **Answer Format:** Writing numbers under the horizontal line.
*   **Structural Quirks:** Vertical arrangement with numbers formatted as "Q[number]." Next to the question.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}