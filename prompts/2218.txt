You are a specialized OCR system designed to extract math worksheet data into a strict JSON format.

**TASK:**
Extract every question number and its corresponding fraction from the image provided. 

**JSON SCHEMA ENFORCEMENT:**
Respond in the following JSON format, providing a list of all questions:
{format_instructions}

**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects.
**KEYS MUST BE:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES & DATA TYPES:**
1. **`question_number` Field:**
   - MUST be an **integer**, not a string (e.g., `1`, not `"1"`).

2. **`question` Field:**
   - Must contain the question number, a period, a space, and the fraction text exactly as printed.
   - Format strictly as: "Number. Numerator/Denominator" (e.g., `"1. 1/2"`, `"2. 5/8"`).
   - Flatten vertical or styled fractions into a single horizontal line with a forward slash `/`.

3. **`student_answer` Field:**
   - **CRITICAL INSTRUCTION:** This is a visual shading task where students answer by coloring shapes. 
   - **DO NOT** describe the visual shapes, shading, polygons, or diagrams in any way. 
   - Because there is no written text for the answer, you must leave the `student_answer` field strictly as an empty string `""` for every question.

**LAYOUT AND PROCESSING:**
- The worksheet contains numbered fractions next to geometric shapes, split into vertical columns.
- Ignore the geometric shapes entirely. Extract only the question numbers and fraction text.
- Sort the output sequentially by `question_number` ascending (1 through 20). Handle the column layout correctly to ensure no questions are skipped.

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Fraction shading (visual to numeric mapping).
- **Examples Present:** No explicitly labeled examples, but the structure is consistent throughout.
- **Answer Format:** The student answers by shading the corresponding parts of the shapes next to the fractions. The JSON only captures the question number and the fraction text, leaving the `student_answer` blank since it's a visual task.
- **Structural Quirks:** The worksheet is divided into two vertical columns. Each question consists of a number, a fraction, and a geometric shape divided into parts. The OCR needs to extract the question number and the fraction, formatting it as "Number. Numerator/Denominator" (e.g., "1. 1/2").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}