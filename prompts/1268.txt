You are a highly accurate OCR system designed to extract handwritten math worksheet data into a precise JSON format.

**TASK:**
Extract all multiplication questions and their final student answers from the provided images. 

**STRICT JSON SCHEMA:**
You MUST output a valid JSON object with a single root key `"questions"`. The value must be an array of objects matching this exact structure:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q 1. 66 x 17",
      "student_answer": "1122"
    }
  ]
}
```

**EXTRACTION RULES & DATA TYPES:**

1. `"question_number"` (Integer):
   - Must be an **INTEGER** (e.g., 1, 2, 21). 
   - Strip the "Q", spaces, and any punctuation. DO NOT output strings like "Q1." or "1.".

2. `"question"` (String):
   - Flatten the vertical multiplication problem into a single horizontal string.
   - **EXACT FORMAT REQUIRED:** `"Q [number]. [top_number] x [bottom_number]"`
   - Examples: `"Q 1. 66 x 17"`, `"Q 21. 99 x 14"`
   - You MUST include a space after the "Q".
   - You MUST include a period and a space after the number.
   - You MUST use a lowercase `"x"` for the multiplication sign, with spaces on both sides. DO NOT use uppercase "X".

3. `"student_answer"` (String):
   - Extract **ONLY the FINAL numerical answer** written at the very bottom, below the solid horizontal line.
   - **CRITICAL:** Completely IGNORE all intermediate calculation steps or scratchpad math. Some questions act as examples and show intermediate multiplication steps. For instance, if you see "462", "660", and "1122" stacked under the problem, your output MUST be ONLY `"1122"`.
   - Do not include checkmarks, circles, crossed-out numbers, or stray marks.
   - Return as a string. If the final answer space is entirely blank, return `""`.

**LAYOUT & HANDWRITING GUIDELINES:**
- The document has a grid layout (5 columns and 4 rows per page). Make sure to track the question numbers accurately across columns and rows.
- Ensure the final array is sorted by `question_number` in ascending order (from 1 to 40).
- Pay extreme attention to messy handwriting: differentiate **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**. 
- Always verify you are pulling the digits from below the lowest solid horizontal line for the `student_answer`.

Return ONLY the valid JSON object. Do not include introductory text, explanations, or dynamic keys.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here is an analysis of the key factors to help write better OCR instructions:

*   **Question Type:** Multiplication (2-digit by 2-digit).
*   **Examples Present:** Yes, Q1, Q6, Q11, Q16, Q21, Q26, Q31, and Q36 are presented with intermediate steps shown, acting somewhat like examples, but they are numbered as regular questions and require extraction of the final answer.
*   **Answer Format:** Students write the final numerical answer below the solid horizontal line at the bottom of each multiplication problem. Some answers are circled or checked by a teacher. Intermediate steps (if written) should be ignored.
*   **Structural Quirks:**
    *   The problems are arranged in a grid with 5 columns and 4 rows per page (totaling 20 questions per page, 40 total).
    *   Questions are numbered explicitly with a "Q" prefix (e.g., "Q 1.", "Q 21.").
    *   The multiplication problems are laid out vertically. The `question` field in the JSON should flatten this into a single horizontal string (e.g., "Q 1. 66 x 17").
    *   Only the final result below the horizontal line should be captured as the `student_answer`.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}