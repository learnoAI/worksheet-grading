You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided.

**WORKSHEET CONTEXT:**
- The worksheet consists of number comparison questions where the student is asked to identify the smaller number.
- The student answers by circling one of the two numbers provided.
- Questions are arranged in two columns.
- Each question has a label starting with "Q" followed by the question number (e.g., "Q1.", "Q2.").

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a JSON array of objects.
{format_instructions}

**KEYS AND DATA TYPES MUST BE EXACTLY:**
1. `"question_number"` (Integer)
2. `"question"` (String)
3. `"student_answer"` (String)

**FORMATTING RULES:**
1. **`question_number` Field (Integer):**
   - MUST be an integer.
   - Strip out any alphabetic characters or punctuation. Extract ONLY the numerical value (e.g., if the printed text is "Q1.", you must output `1`).
2. **`question` Field (String):**
   - Must include the full label and both numbers separated by a comma (e.g., "Q1. 30 , 29").
   - Must flatten vertical text into a single horizontal line.
   - **NO** newline characters in the string.
3. **`student_answer` Field (String):**
   - Extract the specific number that the student has circled in pencil.
   - Return the answer as a string (e.g., "29").
   - If no number is circled or the answer slot is blank, use an empty string `""`.

**HANDWRITING EXTRACTION & LAYOUT GUIDELINES:**
- Ignore any prominent red checkmarks or grading marks across the page; focus entirely on the printed text and pencil circles.
- Look closely at digit formation (e.g., distinguish 9 vs 8, 6 vs 0, 1 vs 7 vs 4).
- Handle the multiple columns correctly by tracking the Question IDs.

**PROCESSING ORDER:**
- Sort the final output array strictly by `question_number` in ascending order.

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Number comparison (circling the smaller number).
- **Examples Present:** None.
- **Answer Format:** The student answers by circling the smaller number out of the two options provided.
- **Structural Quirks:** The questions are arranged in two columns. Each question has a label starting with "Q" followed by the question number (e.g., Q1., Q2.). There's a vertical dividing line between the columns on the first page, and a prominent red checkmark across the page.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}