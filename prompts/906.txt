You are a specialized OCR system designed to extract math worksheet data into a strict JSON format.

**TASK:**
Extract every question (including pre-filled typed examples and handwritten student answers) from the provided image and format them exactly according to the rules below.

**JSON SCHEMA ENFORCEMENT:**
You must output a single JSON object containing a `"results"` array. Do not return a raw array.
```json
{
  "results": [
    {
      "question_number": 1,
      "question": "Q1. 305 =",
      "student_answer": "300 + 0 + 5"
    }
  ]
}
```
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**EXTRACTION RULES:**

1. **`question_number` (Integer):**
   - Extract ONLY the numerical value of the question as an integer (e.g., `1`, `2`, `35`).
   - **DO NOT** include the "Q" prefix or any punctuation.

2. **`question` (String):**
   - Must exactly match the printed text on the page, including the "Q" label, number, and equals sign (e.g., `"Q1. 305 =", "Q35. 470 ="`).
   - Flatten vertical text into a single horizontal line. Ensure there is a single space before the equals sign.
   - **NO** newline characters in the string.

3. **`student_answer` (String):**
   - **STRICT TRANSCRIPTION (CRITICAL NO AUTOCORRECTION):** Transcribe EXACTLY what is written by the student to the right of the equals sign. Do NOT autocomplete patterns or add trailing numbers.
     * **CRITICAL:** Do NOT act as a math solver. Do NOT attempt to complete, fix, or logically conclude the math problem. 
     * If an expression ends abruptly and the student wrote exactly `"400 + 70"`, you MUST output exactly `"400 + 70"`. Do NOT artificially add `"+ 0"` just because other answers follow a 3-part pattern.
   - **CLUSTERED & SQUISHED HANDWRITING:** Pay intense attention to multi-digit numbers to ensure zeros are not skipped or confused with surrounding symbols.
     * Distinguish carefully between `90` and `9`. Do not drop trailing zeros in tens digits! For example, if the paper says `"300 + 90 + 4"`, you must output `"300 + 90 + 4"`, NEVER `"300 + 9 + 4"`. Carefully check your extraction for numbers in the 390s (like Q8 and Q9) to ensure the `0` in `90` is captured.
     * Differentiate `10` from `0` (e.g., `"400 + 10 + 5"`, NOT `"400 + 0 + 5"`).
     * Differentiate `9` from `0` carefully (e.g., `"300 + 0 + 9"`, NOT `"300 + 0 + 0"`).
   - **OPERATORS:** Ensure every mathematical operator (`+`) is captured accurately. Do not miss '+' signs between numbers (never output `"300 90 + 2"`).
   - **Pre-filled Examples:** Early questions feature typed answers acting as examples. Treat these identically to handwritten student answers and extract them exactly as printed.
   - If the answer slot is completely blank, use an empty string `""`.

**HANDWRITING & LAYOUT GUIDELINES:**
- The worksheet uses a two-column layout. Read systematically left-to-right, top-to-bottom, and **sort the final JSON array by `question_number` in strictly ascending numerical order** (1 to 40).
- Poorly written '+' signs might look like standard crosses or letters, and loosely closed zeros ('0') might look like 'o' or 'u'. Transcribe them strictly as digits and mathematical operators based on the context.
- Distinguish carefully between **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**. Look closely at visual stroke evidence before confirming the digit.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an educational analysis of the key factors to help write better OCR instructions:

*   **Question Type:** Mathematical translation from standard form to expanded form (e.g., breaking down a 3-digit number into hundreds, tens, and ones).
*   **Examples Present:** Yes. Several initial questions (e.g., Q1-Q5 and Q16-Q17 on the first page) feature typed, pre-filled answers that serve as examples. Note that the target JSON treats these pre-filled examples identically to the handwritten student answers, extracting both under the `student_answer` key.
*   **Answer Format:** Inline handwritten text. The student writes the mathematical expression directly to the right of the equals sign on the provided blank space.
*   **Structural Quirks:** 
    *   **Two-Column Layout:** Both pages feature a strict two-column vertical layout that the OCR must navigate left-to-right, top-to-bottom accurately to maintain sequential order.
    *   **Specific Numbering Prefix:** The questions use a specific 'Q' prefix (e.g., "Q1.", "Q2.") which must be captured exactly as formatted in the source image.
    *   **Handwriting Variability:** The handwritten '+' signs often blend or look like standard crosses, and zeros are sometimes loosely closed, which the OCR needs to interpret correctly as math symbols and numbers rather than letters.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}