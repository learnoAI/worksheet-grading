You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. The worksheet consists of vertical addition problems involving two-digit numbers added to 110, arranged in a grid layout.

**JSON SCHEMA ENFORCEMENT:**
You MUST output a single JSON object with a root key `"questions"`. The value must be an array of objects. Do NOT return a raw array.
Ensure your response exactly matches this structure and uses these specific data types:

```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 110 + 50",
      "student_answer": "160"
    }
  ]
}
```

**FORMATTING RULES:**
1.  **`question_number` (Integer):**
    - Extract ONLY the integer value from the question label. For example, if the worksheet says "Q1.", the value must be `1` (as a number, not a string).
2.  **`question` (String):**
    - You MUST include the question label at the start (e.g., "Q1.", "Q2.").
    - Flatten the vertical addition problem into a single horizontal string.
    - **CRITICAL SPACING:** You MUST insert a single space before and after the mathematical operator.
    - The format must strictly follow the pattern: `"[Label] [top_number] + [bottom_number]"` (Example: `"Q1. 110 + 50"`).
    - Do NOT include newline characters.
3.  **`student_answer` (String):**
    - Extract exactly what the student wrote underneath the horizontal line indicating the sum.
    - Output this value as a string (e.g., `"160"`).
    - If the answer slot is blank, use an empty string `""`.

**HANDWRITING & LAYOUT EXTRACTION GUIDELINES:**
- The document has multiple columns and rows. Process them carefully by tracking the "Q[num]." labels to ensure no problems are missed.
- Look closely at digit formation. Distinguish **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**.
- If a number is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER:**
- Extract all questions present.
- Sort the final `"questions"` array by `question_number` in ascending order (e.g., 1, 2, 3... up to 40).

=== WORKSHEET CONTEXT NOTES ===
Here is a breakdown of the worksheet's key features to assist with OCR instructions:

*   **Question Type:** Vertical addition problems involving two-digit numbers added to 110.
*   **Examples Present:** None explicitly marked as examples, but the consistent format acts as a template.
*   **Answer Format:** Students write their numerical answers underneath a horizontal line indicating the sum.
*   **Structural Quirks:**
    *   The problems are arranged in a grid-like layout with four columns and five rows on each page.
    *   Each problem is numbered with a "Q" followed by the number (e.g., Q1., Q21.).
    *   The addition is formatted vertically with the "Q[num]." label positioned to the left of the operation.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}