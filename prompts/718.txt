You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided images. 

**WORKSHEET CONTEXT & LAYOUT:**
- The task involves multiplication problems with fill-in-the-blank answers (handwritten numbers next to the equals sign).
- The questions are arranged in two vertical columns per page.
- You will be processing two pages/images: one containing Q1-Q20 and the other containing Q21-Q40. You must combine all data into a single continuous list sorted from 1 to 40.
- **IGNORE** any teacher grading marks (such as large red checkmarks, annotations, or overall scores).

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a JSON object containing a single root key `"box_2d"`, which maps to an array of objects.
**DO NOT** return a raw array. 
**DO NOT** use dynamic keys. 
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

Required Output Structure:
{
  "box_2d": [
    {
      "question_number": 1,
      "question": "Q1. 6 x 3 =",
      "student_answer": "18"
    },
    ...
  ]
}

**FORMATTING RULES:**
1. **"question_number" (Integer):**
   - Must be a strict **INTEGER** type (e.g., 1, 2, 22). 
   - **DO NOT** include the "Q" prefix in this field (e.g., return `1`, not `"Q1"`).
2. **"question" (String):**
   - Must include the full printed text, including the label exactly as it appears (e.g., "Q1. 6 x 3 =").
   - Must flatten any vertical alignment into a single horizontal string.
   - **NO** newline characters in the string.
3. **"student_answer" (String):**
   - Extract EXACTLY what the student wrote in pencil next to the equals sign.
   - **CRITICAL ANTI-HALLUCINATION RULE:** Transcribe the student's answer literally. **DO NOT** attempt to solve the math problems or correct incorrect answers. For example, if the student wrote "35" for "6 x 5 =", you MUST transcribe "35".
   - Always return the answer as a string.
   - If the answer slot is completely blank, use an empty string `""`.

**HANDWRITING EXTRACTION GUIDELINES:**
- Base your transcription purely on visual strokes, not mathematical correctness.
- Look closely at digit formation to distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.

**PROCESSING ORDER:**
- Extract all questions from both images (Q1 through Q40).
- Sort the final array inside `"box_2d"` by `question_number` in strict ascending order (1 to 40).
- Ensure both columns on both pages are fully processed.

Output ONLY valid JSON matching the exact schema above. Do not include markdown formatting or conversational text.

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Multiplication.
*   **Examples Present:** None.
*   **Answer Format:** Fill in the blanks (handwritten numbers next to the equals sign).
*   **Structural Quirks:**
    *   Questions are arranged in two vertical columns.
    *   The question numbers across the two images form a single sequence from Q1 to Q40.
    *   The second image (horizontal) contains Q1-Q20, while the first image (vertical) contains Q21-Q40.
    *   The target JSON must combine the questions from both images into a single continuous sequence, ordering them by the printed question numbers (Q1, Q2, ..., Q40) rather than by reading left-to-right, top-to-bottom across the whole set. It essentially involves treating the first provided image as page 2 and the second as page 1.
    *   A teacher's grading mark (a large red checkmark and score) is visible on both pages. Ignore this.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}