You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every vertical subtraction question and its corresponding handwritten student answer from the provided image. 
- **IGNORE** header fields (Name, Token Number, Marks) and any stray handwritten numbers at the top (e.g., '3' or '4').
- All problems are student-answered questions; there are no pre-solved examples.
- **CRITICAL: DO NOT SOLVE THE MATH.** Transcribe the student's answer EXACTLY as written in pencil underneath the line, even if their math is completely incorrect. Your job is pure visual transcription, not calculation. 

**BEWARE OF MATH BIAS (CRITICAL WARNING):**
LLMs naturally want to solve math problems. You must actively suppress this. 
- Example 1: If the problem is 212 - 110 (math = 102) and the student wrote "002", you MUST output "002".
- Example 2: If the problem is 221 - 110 (math = 111) but the student wrote "112", you MUST output "112". Look purely at the pencil strokes, NOT the expected arithmetic result.

**JSON SCHEMA ENFORCEMENT:**
Respond with a JSON array containing exactly 40 objects, formatted like this:
[
  {
    "question_number": 1,
    "question": "Q1. 211 - 110",
    "student_answer": "101"
  }
]

**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects.
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES:**
1.  **Question Number Field (`question_number`):**
    - MUST be an **INTEGER** (e.g., `1`, `2`, `11`).
    - Extract ONLY the numeric value from the question label. Strip the "Q" prefix and the period "." (e.g., "Q1." becomes `1`).
2.  **Question Field (`question`):**
    - MUST be a STRING.
    - MUST include the exact label (e.g., "Q1.", "Q2.") at the start.
    - Translate the spatial vertical math (3-digit minus 3-digit) into a single linear horizontal string (e.g., "Q1. 211 - 110").
    - **NO** newline characters in the string.
3.  **Student Answer Field (`student_answer`):**
    - Extract exactly what the student wrote directly below the horizontal line.
    - Return the answer as a STRING.
    - If the answer slot is completely blank, use an empty string `""`.
    - **DO NOT AUTO-CORRECT.** Maintain all leading zeros or incorrect digits exactly as formed by the student.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look extremely closely at digit formation to avoid transcription errors. Do not let mathematical logic override visual evidence.
- **PAY EXTREME ATTENTION TO THE LAST DIGIT:** Students frequently make errors in the ones column. Carefully examine the final digit of every handwritten answer.
- **Distinguish 1 vs 2:** This is the most common point of failure. Carefully check if the stroke hooks at the top or has a horizontal base (making it a 2), or if it is a single straight vertical line (making it a 1). 
- **Distinguish 1 vs 9:** Look for a closed top loop (9) vs a simple hook/line (1).
- **Distinguish 9 vs 8, 6 vs 0, and 1 vs 7 vs 4.**
- If a number is messy, select the most likely digit based *purely on visual stroke evidence*, bypassing any math logic.

**PROCESSING ORDER:**
- The questions are arranged tightly in a four-column grid. Read top-to-bottom, column-by-column, or strictly follow the numerical sequence of the "Q" labels.
- Track the question IDs carefully to handle the columns correctly.
- Sort the final output strictly by `question_number` in ascending order (1 to 40).
- Ensure exactly 40 items are extracted and returned.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here is an analysis of the key factors to optimize OCR instructions:

*   **1. Question Type:** Vertical subtraction (3-digit minus 3-digit numbers).
*   **2. Examples Present:** None. All problems on the page are student-answered questions; there are no pre-solved examples.
*   **3. Answer Format:** Handwritten digits placed directly below the horizontal line of each vertical subtraction problem.
*   **4. Structural Quirks:**
    *   **Grid Layout:** The problems are arranged tightly in a four-column format.
    *   **Spatial to Linear Translation:** The questions are formatted vertically on the page but must be transcribed horizontally in the JSON `question` field (e.g., translating the stacked "211 over - 110" to the linear string "Q1. 211 - 110").
    *   **Specific Numbering Prefix:** Question numbers sit to the left of the math problem and follow a strict "Q1.", "Q2.", etc. format, which needs to be prepended to the math equation in the output.
    *   **Ignored Content:** Header fields (Name, Token Number, Marks) and stray handwritten numbers at the top (like '3' or '4') are excluded from the target output.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}