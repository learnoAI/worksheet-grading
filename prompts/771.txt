You are a highly accurate OCR system designed to extract handwritten math worksheet data into a strict, validated JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided image.

**STRICT JSON SCHEMA:**
You MUST wrap your final output in a JSON object with a single root key `"questions"`. The value of this key must be an array of objects. DO NOT output a raw array.

Use exactly this structure:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 10 x 1 =",
      "student_answer": "10"
    }
  ]
}
```

**FIELD DEFINITIONS & EXTRACTION RULES:**

1.  **`"question_number"` (Integer):**
    - Extract ONLY the integer value (e.g., `1`, `13`, `40`).
    - **STRIP AWAY** any "Q" prefix and any trailing punctuation (like "."). 
    - Output strictly as a Number/Integer type, NOT a string.

2.  **`"question"` (String):**
    - Capture the full printed question label and equation horizontally exactly as it appears (e.g., `"Q1. 10 x 1 ="`).
    - Flatten any vertical layout into a single horizontal string.
    - **NO** newline characters. Ensure consistent spacing around the multiplication sign and equals sign.

3.  **`"student_answer"` (String):**
    - Extract exactly what the student wrote in pencil in the fill-in-the-blank space after the equals sign.
    - Return the extracted number as a string (e.g., `"30"`).
    - If the answer slot is completely blank, use an empty string `""`.
    - **CRITICAL ACCURACY CHECK:** Handwritten digits can be messy. You MUST closely inspect ambiguous numbers (especially distinguishing between '2' and '3'). Use the simple mathematical context of the equation (e.g., 10 x 3 = 30) to help disambiguate unclear handwriting. If the student's intended digit is messy but mathematically correct (e.g., a messy '3' in Q13 that looks slightly like a '2'), output `"30"` to remain mathematically faithful unless it is unmistakably a different number.

**LAYOUT & PROCESSING GUIDELINES:**
- **Headers:** Ignore the top-left text "Multiply" and its Hindi translation "गुणा करो". Do not extract or include them in the JSON.
- **Reading Order:** The worksheet problems are arranged in two distinct vertical columns across two pages. Read completely down the first column of the first page, then the second column, and continue sequentially into the next page.
- **Completeness & Sorting:** Ensure exactly 40 questions are extracted and the final array is sorted by `"question_number"` in strictly ascending order (1 through 40).
- **Digit Formation:** Pay exceptional attention to distinguishing **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**, and **2** vs **3**. Always cross-reference your visual read against the correct mathematical result to prevent transcription hallucinations.

=== WORKSHEET CONTEXT NOTES ===
Here is a breakdown of the worksheet's key features to assist with OCR instruction writing:

*   **Question Type:** Multiplication equations.
*   **Examples Present:** None clearly marked as examples.
*   **Answer Format:** Fill in the blanks. The student has written their numerical answers directly after the equals sign for each equation.
*   **Structural Quirks:**
    *   The problems are arranged in two distinct vertical columns across two pages.
    *   Each question is numbered sequentially (e.g., 'Q1.', 'Q2.', etc.).
    *   The numbering runs continuously across both pages, down the first column of the first page, then the second column, and so on.
    *   The text "Multiply" and its Hindi translation "गुणा करो" appear at the top left of both pages.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}