You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer (the encircled number) from the provided image. Ignore page headers such as Name, Token Number, Marks, and Number Order.

**JSON SCHEMA ENFORCEMENT:**
Your entire output MUST be a single JSON object with a root key named `"worksheet_data"`, which contains an array of the extracted questions.
**DO NOT** return a raw array. 
{format_instructions}

**REQUIRED STRUCTURE:**
```json
{
  "worksheet_data": [
    {
      "question_number": 1,
      "question": "Q1. 4 , 2",
      "student_answer": "2"
    }
  ]
}
```
**KEYS MUST BE EXACTLY:** `"question_number"` (integer), `"question"` (string), `"student_answer"` (string).

**FORMATTING & EXTRACTION RULES:**
1.  **Question Field (`question`):**
    - Must include the label exactly as written (e.g., "Q1.", "Q2.") at the start.
    - **CRITICAL SPACING RULE:** Transcribe the question exactly as it appears, preserving all original spacing. The options are separated by a space, a comma, and another space. You MUST include the space before the comma (e.g., `"Q1. 4 , 2"`). **DO NOT** output `"Q1. 4, 2"`.
    - Flatten vertical text into a single horizontal line.
    - **NO** newline characters in the string.
2.  **Student Answer Field (`student_answer`):**
    - The correct answer format is indicated by the student **circling** one of the numbers. Extract exactly the number that is circled.
    - If the answer slot is blank or uncircled, use an empty string `""`.
    - Return the answer as a string.
3.  **Handwriting Accuracy Guidelines:**
    - Look closely at handwritten digit formation.
    - Carefully double-check the student answers to differentiate between similar-looking digits like **3** and **4** (pay special attention to questions like Q9), **9** vs **8**, **6** vs **0**, and **1** vs **7**.
    - If a number is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER:**
- The worksheet contains questions split across two distinct vertical columns.
- Read sequentially down the first column, then down the second column.
- Sort the final output by `question_number` in ascending order (1 to 40).

=== WORKSHEET CONTEXT NOTES ===
Here is the analysis of the worksheet based on the requested factors:

*   **Question Type:** Comparison (identifying the smaller number from a pair).
*   **Examples Present:** None.
*   **Answer Format:** Circling the correct number out of two options.
*   **Structural Quirks:** Two distinct vertical columns of questions. Questions are numbered sequentially across the two pages (Q1-Q40), starting down the first column, then the second column on each page. The question text format includes the options separated by commas (e.g., "Q1. 4 , 2"). Some handwritten numbers are encircled, indicating the student's answer. The page also features a header with fields for Name, Token Number, Marks, and "Number Order".
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}