You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet images. The images consist of two pages containing vertical addition problems (Q1-Q20 on the first page, Q21-Q40 on the second page).

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a valid JSON object containing a single root key `"data"` that wraps the list of question objects. 
{format_instructions}

**REQUIRED JSON STRUCTURE:**
```json
{
  "data": [
    {
      "question_number": 1,
      "question": "Q1. 355 + 35",
      "student_answer": "390"
    }
  ]
}
```

**FIELD EXTRACTION RULES:**
1.  **`question_number` (Integer):**
    - MUST be an **integer** (e.g., `1`, `2`, `40`). 
    - **DO NOT** output a string (e.g., "Q1." is incorrect, `1` is correct).
2.  **`question` (String):**
    - MUST begin with the uppercase letter 'Q', the question number, a period, and a space (e.g., "Q1. ", "Q40. ").
    - MUST flatten the vertical math problem into a single horizontal string (e.g., "Q1. 355 + 35").
    - **NO** newline characters in the string.
3.  **`student_answer` (String):**
    - Extract exactly what the student wrote in pencil below the horizontal line.
    - Return the answer as a string.
    - If the answer slot is completely blank, use an empty string `""`.

**LAYOUT & ANTI-HALLUCINATION GUIDELINES:**
- **Read Sequentially:** The questions are arranged in a grid with four columns per page. Read down each column sequentially.
- **Two Pages:** Remember that the first page contains Q1-Q20, and the second contains Q21-Q40. Combine all 40 questions into the single `"data"` list.
- **Do Not Duplicate:** Transcribe the exact text for every question sequentially. Pay special attention to the end of pages (e.g., Q20, Q40). **DO NOT** duplicate earlier questions (like repeating Q20 in place of Q40) just to fill out the list.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation in the student answers.
- Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
- If a number is messy, select the most likely digit based on visual stroke evidence.

=== WORKSHEET CONTEXT NOTES ===
Here is your analysis of the worksheet images:

*   **Question Type:** Vertical addition problems (3-digit + 2-digit numbers).
*   **Examples Present:** None explicitly marked as examples, but the structure is very consistent throughout.
*   **Answer Format:** Students write their numerical answers below the horizontal line of each addition problem. Some answers appear to be printed, while others are handwritten.
*   **Structural Quirks:**
    *   Questions are arranged in a grid-like format with four columns per page.
    *   Each question is numbered sequentially (e.g., 'Q1.', 'Q2.').
    *   The question text is formatted vertically on two lines, with a '+' sign before the second number.
    *   The target JSON formats the question horizontally (e.g., "Q1. 355 + 35").
    *   The image consists of two separate pages. The first page contains Q1-Q20, and the second contains Q21-Q40. The JSON combines them into a single list.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}