You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every subtraction question and its corresponding student answer from the provided worksheet image. There are exactly 40 questions on this page.

**LAYOUT & NAVIGATION (CRITICAL):**
- The problems are arranged in a grid with columns. 
- You MUST scan the document systematically. Carefully read the columns strictly from left to right (read completely down the leftmost column first, then move to the next column to the right).
- **DO NOT SKIP ANY QUESTIONS.** Ensure you capture the entire document, specifically verifying you have not skipped the top or bottom of any column.
- **SEQUENTIAL ORDERING:** You MUST sort the final JSON array sequentially by `question_number`, starting from 1 up to 40. Do not output Q21-Q40 before Q1-Q20. Double-check your list numbering to ensure sequential accuracy without repeating or skipping numbers.

**JSON SCHEMA & DATA TYPES:**
Respond EXACTLY with a JSON array containing objects with the following strict keys and data types:
- `"question_number"`: **(Integer)** The pure numeric ID of the question. You MUST strip the "Q" prefix and any punctuation (e.g., output `1`, NOT `"Q1"` or `"Q1."`).
- `"question"`: **(String)** The full question text, starting with the exact label, flattened horizontally. Use the format "Q#. [Top Number] - [Bottom Number]" (e.g., `"Q1. 474 - 98"`).
- `"student_answer"`: **(String)** The handwritten answer written directly below the line. 

**CRITICAL EXTRACTION RULES:**
1. **NO AUTO-CORRECTION OF MATH:** You MUST transcribe the student's answer exactly as written, even if it is mathematically incorrect or contains extra digits. For example, if the math is 233 - 46 and the student wrote "1187", you MUST output `"1187"`, not `"187"`. Do not evaluate the math.
2. **STRICT VISUAL NUMBERING (NO RENUMBERING):** Transcribe the question numbers *exactly* as they visually appear on the page. **DO NOT** auto-correct or blindly re-number lists if you misread a number. If the text visually says "Q21", you must extract `21`. Never change a visually confirmed "Q21" to "Q1". 
3. **FLATTEN VERTICAL TEXT:** The worksheet displays vertical subtraction. You must flatten this into a single horizontal line with spaces around the minus sign (e.g., `"Q20. 233 - 46"`). Do NOT include newline characters (`\n`).
4. **BLANK ANSWERS:** If the student's answer slot (below the subtraction line) is totally blank, use an empty string `""`.

**HANDWRITING GUIDELINES:**
- Look closely at digit formation: distinguish carefully between **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**.
- If a number is messy, select the exact digits based purely on visual stroke evidence, not logical deduction.

**OUTPUT FORMAT:**
Return ONLY a valid, well-formed JSON array of exactly 40 objects. Ensure the array is ordered sequentially by `question_number` from 1 to 40.
[
  {
    "question_number": 1,
    "question": "Q1. 474 - 98",
    "student_answer": "376"
  },
  {
    "question_number": 2,
    "question": "Q2. 832 - 65",
    "student_answer": "767"
  },
  ...
]

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Subtraction (vertical format).
*   **Examples Present:** None.
*   **Answer Format:** Written below the subtraction line.
*   **Structural Quirks:** Problems are arranged in a grid with numbered questions (e.g., 'Q1.', 'Q2.'). Each problem is a subtraction of a two-digit number from a three-digit number.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}