You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet image. 

**WORKSHEET CONTEXT & RULES:**
- **Layout:** The page contains vertical 3-digit addition problems arranged in a 4-column grid. Read them carefully row by row or column by column, but order your final output sequentially by the printed question numbers (1 to 40).
- **Headers to Ignore:** Do NOT extract page headers such as Name, Token Number, Marks, "Addition - 1648", or any general instructions.
- **Carry-over Marks:** IGNORE any small, circled, or lightly handwritten numbers at the top of the columns used by the student for carry-overs. Only extract the primary printed question operands and the final answer.
- **NO AUTOCORRECTION:** Strictly transcribe the student's answers exactly as they appear on the page. Do NOT calculate, fix, or autocorrect any mathematical errors made by the student. You are transcribing raw text, not grading the math.

**DATA EXTRACTION & FORMATTING RULES:**
1.  **`question_number` (Integer):** 
    - Extract strictly as an **integer** by removing any 'Q' prefixes (e.g., output `1` instead of `"Q1"`).
2.  **`question` (String):** 
    - You MUST flatten the vertical printed problem into a single horizontal string.
    - Start the string with the question label (e.g., `"Q1. "`).
    - Format it as `"[Label] [Top Number] + [Bottom Number]"` (e.g., `"Q1. 110 + 190"`).
    - **NO** newline characters (`\n`) are allowed in this string.
3.  **`student_answer` (String):**
    - Extract exactly what the student wrote in pencil beneath the solid horizontal line.
    - Transcribe what you see visually. If the math is wrong (e.g., student wrote "204" but the actual sum is 304), output the incorrect handwritten "204".
    - If the answer slot is blank (unanswered), use an empty string `""`.
    - Always return this field as a string.
    - Pay close attention to digit formation (distinguish 9 vs 8, 6 vs 0, 1 vs 7 vs 4).

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a single JSON object containing a root key `"questions_and_answers"`, which maps to a list of objects. Do not use dynamic keys or nested objects beyond this structure.

**REQUIRED JSON STRUCTURE:**
{
  "questions_and_answers": [
    {
      "question_number": 1,
      "question": "Q1. 110 + 190",
      "student_answer": "300"
    },
    {
      "question_number": 2,
      "question": "Q2. 111 + 190",
      "student_answer": "301"
    }
  ]
}

Return ONLY the valid JSON object. Do not include markdown formatting like ```json ... ``` or any explanatory text. Ensure proper escaping and valid syntax.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON, here is an analysis of the key factors to guide OCR instructions:

*   **Question Type:** Vertical addition problems involving 3-digit numbers.
*   **Examples Present:** None. There are no pre-solved examples; all items (Q1-Q40) are standard problems for the student to complete.
*   **Answer Format:** Handwritten numbers placed beneath a solid horizontal line indicating the sum.
*   **Structural Quirks:**
    *   **Grid Layout:** The problems are arranged in four distinct vertical columns across the page.
    *   **Vertical to Horizontal Transcription:** While printed vertically on the page, the target JSON requires the question to be transcribed as a single horizontal string (e.g., vertically stacked `110`, `+ 190` becomes `"Q1. 110 + 190"`).
    *   **Operator Placement:** The addition symbol (`+`) is printed on the second line, directly to the left of the bottom operand.
    *   **Ignore Carry-over Marks:** The student has written small, circled numbers at the top of many problems to indicate carry-overs. These handwritten annotations must be completely ignored during extraction.
    *   **Handling Blanks:** Unanswered questions (e.g., Q40) must be captured with an empty string (`""`) for the `student_answer` field.
    *   **Ignore Headers:** Page headers (Name, Token Number, Marks, "Addition - 1648", instructions) are excluded from the target data structure.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}