You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every long division question and its corresponding final answer from the image provided. 

**WORKSHEET LAYOUT & CONTEXT:**
- The page contains exactly 40 problems arranged in a multi-column grid layout.
- Every problem starts with an explicit label like "Q1.", "Q2.", etc.
- Format is standard long division: `divisor ) dividend`.
- Students show vertical subtraction steps below the dividend. You MUST entirely ignore these steps.
- The final answer (quotient) is typically written by the student to the right of the dividend (often next to a curved bracket) or printed above the division bar.

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a single JSON object with a root key called `"questions"` that contains an array of the results. Do NOT output a raw JSON array. Use exactly this structure:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 8 ) 928",
      "student_answer": "116"
    }
  ]
}
```
**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects.
**KEYS MUST BE STRICTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES:**
1.  **Question Field (`question`):**
    - Must exactly follow this strict format: `"Q[number]. [divisor] ) [dividend]"`
    - **CRITICAL SPACING:** You MUST include a single space before and after the parenthesis. Example: `"Q1. 8 ) 928"` (Do NOT output `"Q1. 8) 928"`).
    - **DO NOT** append the student's answer, quotient, or any trailing parentheses/brackets to this field. The question string must end immediately after the dividend.
    - **NO** newline characters.
2.  **Student Answer Field (`student_answer`):**
    - Extract **ONLY the final quotient** (the final answer to the division problem).
    - **CRITICAL:** Completely IGNORE all vertical step-by-step mathematical working (subtractions, carried down numbers, etc.) shown below the dividend.
    - **DO NOT** include newline characters (`\n`), mathematical steps, or multiple numbers. The answer must be a single straightforward number string (e.g., `"116"`, not `"116\n8\n48\n48\n0"`).
    - If an answer is printed above the line and not rewritten by the student, extract the printed one.
    - If the final answer slot is completely blank, use an empty string `""`.

**ANTI-HALLUCINATION & INTERNAL VERIFICATION:**
- **ANCHOR TO LABELS:** Always locate the exact "Q[#]." text first to ensure you are extracting the correct divisor and dividend for that specific question number. Do not accidentally pull numbers from adjacent columns (e.g., do not put Q7's numbers into Q2).
- **MATH VERIFICATION STEP (SELF-CORRECTION):** Before outputting, internally calculate `Divisor * Final Quotient`. If it does not equal the `Dividend`, you have likely made a visual transcription error. 
    - *Example:* If you transcribe divisor `9`, dividend `527`, and quotient `63`, check the math: `9 * 63 = 567` (not `527`). Look closely at the image again: is the `5` actually a `9`? (Targeting `927 / 9 = 103`).
    - Use this step **ONLY to double-check your transcription**. Never invent an answer to make the math work, and never auto-calculate an answer if the student left it blank. Transcribe exactly what is visibly written.
- **VISUAL SIMILARITIES:** Pay extreme attention to easily confused digits, explicitly double-checking:
    - **9 vs. 5** (Highly common error on this worksheet)
    - **6 vs. 7** 
    - **9 vs. 8**
    - **6 vs. 0**
    - **1 vs. 7 vs. 4**

**PROCESSING ORDER:**
Scan carefully through the grid. Ensure you capture all 40 questions and sort the final output strictly by `question_number` in ascending order (1 to 40). Verify your sequence has no missing or duplicated numbers.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here is an analysis of the key factors to guide the OCR instructions:

*   **Question Type:** Long division problems.
*   **Examples Present:** None explicitly marked as examples, but the layout is consistent throughout. Some printed answers appear above the division bar (e.g., Q1, Q6, Q11, Q16, Q21), but the student's handwritten final answer is the primary target.
*   **Answer Format:** Handwritten answers are predominantly written to the right of the dividend, typically enclosed in parentheses or a curve (e.g., `(125`). In some cases, a printed answer exists above the division bar. The target output extracts the final quotient written by the student.
*   **Structural Quirks:**
    *   The problems are arranged in a grid-like layout with four vertical columns per page.
    *   Each question starts with a specific numbering format (e.g., "Q1.", "Q 2.").
    *   The problem format is standard long division notation: `divisor ) dividend`.
    *   Students show their working (subtraction steps) vertically below the dividend, which is *not* required in the target JSON. Only the final quotient (usually written next to the dividend in brackets) needs to be extracted as the `student_answer`. If an answer is printed above the line and not rewritten by the student, extract the printed one.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}