You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every division question and its corresponding handwritten student answer from the provided images. 

**JSON SCHEMA ENFORCEMENT:**
You MUST output a valid JSON object containing a single root key `"questions"`, which holds an array of objects. Do not return a raw array.
Use exactly this structure:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q 1. 42 ÷ 6 =",
      "student_answer": "7"
    }
  ]
}
```

**FIELD EXTRACTION RULES:**

1. **`question_number` (Integer):**
   - Extract ONLY the integer value for the question number.
   - Drop any "Q", periods, or spaces. (e.g., if the page says "Q 1.", output the integer `1`).

2. **`question` (String):**
   - Transcribe the printed text exactly, preserving the structure: "Q {number}. {dividend} ÷ {divisor} =".
   - **Spacing is critical:** Ensure there is a space between the "Q" and the number (e.g., "Q 1. 42 ÷ 6 =", NOT "Q1.").
   - Use the standard division symbol `÷` (`\u00f7`).
   - **ANTI-HALLUCINATION WARNING:** Transcribe the *exact* numbers printed on the page, even if an equation appears mathematically incorrect. **DO NOT auto-correct the math.** Pay special attention to reading the divisors accurately (e.g., carefully distinguish a printed `6` from a `4`).
   - Flatten vertical or misaligned text into a single horizontal string. No newline characters.

3. **`student_answer` (String):**
   - Extract exactly the digit(s) the student wrote in pencil after the equals sign.
   - Return the answer as a string (e.g., `"7"`).
   - If the answer slot is blank, return an empty string `""`.

**LAYOUT & PROCESSING GUIDELINES:**
- **Two-Column Layout:** The problems are arranged in two columns. Ensure you capture both columns comprehensively.
- **Sorting:** The provided images may be out of order (e.g., questions 21-40 first, then 1-20). You MUST sort the final JSON array by `question_number` in strict ascending order (from 1 to 40).
- **Handwriting Distinctions:** Look closely at handwritten strokes to distinguish **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**. 
- **Noise Rejection:** Ignore large handwritten checkmarks or grading lines drawn across the page. Only extract the math problems and the student's numerical answers.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON, here are the key factors to consider for OCR instructions:

*   **Question Type:** Simple division problems.
*   **Examples Present:** None.
*   **Answer Format:** Handwritten numbers placed to the right of the equals sign for each problem.
*   **Structural Quirks:**
    *   The problems are arranged in a two-column layout.
    *   Each question starts with a specific numbering format: "Q" followed by the number, a period, and then the problem (e.g., "Q 1. 42 ÷ 6 =").
    *   There is a handwritten checkmark line across the middle of the second page.
    *   The first image shows the second half of the worksheet (Questions 21-40), while the second image shows the first half (Questions 1-20). The target JSON orders them sequentially from 1 to 40.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}