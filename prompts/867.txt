You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding handwritten student answer from the provided math worksheet. The worksheet contains basic division problems arranged in two vertical columns. Ignore general page headers like "Divide / भाग करो".

**JSON SCHEMA ENFORCEMENT:**
You MUST output a single JSON object with a root key `"questions"` containing an array of objects.
Do not wrap the output in markdown code blocks or add any conversational text. Use exactly this structure:

```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 30 \u00f7 3 =",
      "student_answer": "10"
    }
  ]
}
```

**FIELD EXTRACTION RULES:**

1. **`question_number` (Integer):**
   - Extract the numeric ID from the question label.
   - **MUST BE AN INTEGER**, not a string (e.g., `1`, not `"1"`).
   - **STRIP AWAY** the "Q" prefix (e.g., if the text says "Q21.", the value must be `21`).

2. **`question` (String):**
   - Must include the full label, the equation, and the equals sign exactly as printed (e.g., `"Q21. 20 ÷ 10 ="`).
   - Use the standard division symbol `÷` (`\u00f7`).
   - Flatten the vertical text into a single horizontal string. **NO** newline characters.

3. **`student_answer` (String):**
   - Extract the handwritten numeric answer written to the right of the equals sign.
   - Output as a string (e.g., `"2"`).
   - If the answer slot is blank, use an empty string `""`.

**HANDWRITING & CONTEXT GUIDELINES (CRITICAL):**
- **Math Context Verification:** These are standard division equations. Use the math to double-check messy handwriting. For example, for "Q21. 20 ÷ 10 =", the mathematical answer is 2. If the student's handwritten answer looks ambiguous (e.g., resembling a "1"), look closely at the stroke evidence—it is highly likely a hastily written "2". Do not blindly guess digits without considering the arithmetic context.
- Distinguish carefully between **9** vs **8**, **6** vs **0**, and **1** vs **2** vs **7** vs **4**.

**PROCESSING ORDER:**
- Scan both vertical columns thoroughly.
- Sort the final `"questions"` array by `question_number` in strict ascending order (1, 2, 3, ..., 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided images and target JSON, here is the educational analysis to help formulate better OCR instructions:

*   **Question Type:** Arithmetic equations specifically focused on basic division.
*   **Examples Present:** None. There are no pre-solved examples provided by the worksheet; all answers appear to be written by the student.
*   **Answer Format:** Fill-in-the-blank style. The student writes a single numeric answer directly to the right of the equals sign (`=`).
*   **Structural Quirks:**
    *   The layout is strictly divided into two vertical columns on each page.
    *   Questions follow a specific numbering prefix format: "Q" followed by the number and a period (e.g., "Q1.", "Q21.").
    *   There is a bilingual instruction header ("Divide / भाग करो") at the top left.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}