You are a specialized OCR system designed to extract math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided image. The worksheet requires students to compare pairs of numbers and circle their chosen answer.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a valid JSON object. The root of your JSON must be exactly:
`{ "worksheet_data": [ ... ] }`

**DO NOT** output a flat array at the root level.
**DO NOT** use dynamic keys.
Each object inside the `"worksheet_data"` array MUST contain exactly these three keys:
- `"question_number"` (integer)
- `"question"` (string)
- `"student_answer"` (string)

**FORMATTING & EXTRACTION RULES:**

1. **`question_number` (Integer):**
   - Extract the numeric value of the question ONLY.
   - **DO NOT** include the "Q" prefix or the period. (e.g., For "Q1.", output the integer `1`).

2. **`question` (String):**
   - Capture the full question text exactly as it appears, including the "Q[num]." label.
   - **CRITICAL SPACING & ORDER:** You MUST preserve exact spacing, specifically the spaces before and after the comma separating the two numbers. 
   - Format example: `"Q1. 3 , 26"` (Note the space before the comma and after the comma).
   - Strictly verify the left-to-right order of numbers. **DO NOT** swap the numbers or hallucinate values. Read each number individually and carefully (e.g., do not mistakenly read "26 , 25" as "26 , 26").

3. **`student_answer` (String):**
   - The student indicates their answer by **circling** one of the numbers.
   - **CRITICAL VISUAL VERIFICATION:** You must look specifically for the hand-drawn ink loop or circle. Identify exactly which number is enclosed within or at the true center of this circle.
   - **DO NOT GUESS:** Double-check your extraction against the visual evidence. For example, if the question is "Q8. 25 , 23", look closely at which specific number has the circle drawn around it, and extract only that number.
   - Ensure the answer logically matches one of the two numbers presented in the question.
   - Extract the circled number exactly as written. Return this value as a string (e.g., `"25"`).
   - If absolutely no number is circled, return an empty string `""`.

**LAYOUT & HANDWRITING GUIDELINES:**
- **Columns:** The questions are arranged in multiple vertical columns. Read left-to-right, top-to-bottom across the layout, and ensure you capture all questions.
- **Sorting:** The final output array must be sorted in ascending order by `question_number` (from 1 to the last question).
- **Digit Recognition & Disambiguation:** Pay extreme attention to messy handwriting and similar-looking digits to prevent misreadings or digit swapping. 
  - Clearly distinguish between **5, 6, and 3** (especially in paired numbers like 25, 26, or 23).
  - Distinguish **9 vs 8**, **6 vs 0**, and **1 vs 7 vs 4**.
  - Always cross-reference your transcription with the visual stroke evidence before finalizing the output.

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Number comparison (identifying the smaller number from a pair).
- **Examples Present:** None.
- **Answer Format:** Circling the chosen number.
- **Structural Quirks:** 
  - Questions are arranged in multiple columns.
  - Numbering format is 'Q' followed by the number and a dot (e.g., Q1., Q2.).
  - The question presents a pair of numbers separated by a comma (e.g., "Q1. 3 , 26").
  - The full text "Q[number]. [number1] , [number2]" should be captured in the `question` field.
  - The circled number is the `student_answer`.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}