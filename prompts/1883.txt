You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every math problem and its corresponding handwritten student answer from the provided worksheet image.

**WORKSHEET CONTEXT & LAYOUT:**
- **Problem Type:** Vertical subtraction math problems.
- **Layout:** The problems are arranged in vertical columns. Sort your output sequentially based on the question numbering (1 to 40).
- **Format Transformation:** The physical worksheet presents the problems vertically (stacked numbers with a horizontal line). You must transcribe these into a horizontal equation string (e.g., top number - bottom number).
- **Handwritten Answers:** The student's answers are written in pencil directly below the horizontal line of each vertical problem. There are no pre-solved examples.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a JSON array of objects. Do not use dynamic keys or nest the objects.
Every object must strictly contain these exactly named keys:
`"question_number"`, `"question"`, `"student_answer"`

**FIELD-BY-FIELD FORMATTING RULES:**

1.  **`question_number` (Integer):**
    *   Ensure this is strictly an **integer** (e.g., `1`, `2`, `3`).
    *   Strip out any "Q" prefixes and periods. Do NOT output "Q1." or "1.".

2.  **`question` (String):**
    *   Must include the full question text and prefix exactly as it appears on the page, followed by a space, followed by the flattened horizontal equation.
    *   **Format:** `"Q[X]. [Top Number] - [Bottom Number]"`
    *   **Example:** `"Q1. 631 - 1"` or `"Q12. 731 - 11"`
    *   **NO** newline characters in the string.

3.  **`student_answer` (String):**
    *   Extract exactly what the student wrote in pencil beneath the horizontal line.
    *   This must be a **string** (e.g., `"630"`).
    *   If the answer slot is completely blank, use an empty string `""`.

**HANDWRITING EXTRACTION GUIDELINES:**
- Pay close attention to digit formation to prevent hallucinations.
- Distinguish carefully between **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**.
- If a number is messy or overlaps the line, select the most likely digit based strictly on visual stroke evidence.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON, here is an analysis of the key factors to help write better OCR instructions:

*   **Question Type:** Vertical subtraction math problems.
*   **Examples Present:** None. There are no pre-solved examples; all answers are handwritten by the student.
*   **Answer Format:** Handwritten numbers placed directly below the horizontal line of each vertical problem.
*   **Structural Quirks:**
    *   **Grid Layout:** The problems are arranged in four vertical columns across the page.
    *   **Formatting Transformation:** The physical worksheet presents the problems vertically (stacked numbers), but the target JSON requires transcribing them into a horizontal equation string (e.g., "631 - 1").
    *   **Numbering:** Questions are numbered using a specific prefix format: 'Q' followed by the number and a period (e.g., "Q1.", "Q2.").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}