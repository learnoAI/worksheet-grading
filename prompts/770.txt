You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet images.

**WORKSHEET CONTEXT & HANDLING:**
- The worksheet contains multiplication problems (e.g., "10 x 1 =").
- The problems are arranged in two columns per page. Read in the correct sequence (by question number).
- Some pages may be oriented horizontally (landscape); ensure you extract all questions from all pages.
- **IGNORE** any large checkmarks drawn across the page. Do not let them interfere with text extraction.

**JSON SCHEMA ENFORCEMENT:**
You MUST output a single JSON object containing a `"questions"` array. 
Do NOT output a raw array. 
Do NOT use dynamic keys. 
Your output must exactly match this structure:

```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 10 x 1 =",
      "student_answer": "10"
    },
    {
      "question_number": 2,
      "question": "Q2. 10 x 2 =",
      "student_answer": "20"
    }
  ]
}
```

**FIELD EXTRACTION RULES:**

1. **`question_number` (Integer):**
   - Must be strictly an **INTEGER**.
   - Extract the numeric value from the printed label. For example, if the text says "Q1.", output `1`. If "Q20.", output `20`. Do NOT output strings like "Q1.".

2. **`question` (String):**
   - Must include the exact printed text, starting with the "Q" label (e.g., "Q1. ").
   - **CRITICAL:** Stop exactly at the equals sign (`=`). 
   - **DO NOT** include the student's handwritten answer in this field. 
   - Example Correct: `"Q5. 10 x 2 ="`
   - Example Incorrect: `"Q5. 10 x 2 = 20"`

3. **`student_answer` (String):**
   - Extract exactly the handwritten numbers the student wrote in pencil after the `=` sign.
   - Return this as a string (e.g., `"10"`, `"20"`).
   - If the answer slot is completely blank, return `""`.
   - Distinguish handwriting carefully (e.g., 9 vs 8, 6 vs 0, 1 vs 7 vs 4).

**PROCESSING ORDER:**
- Sort the final array strictly by `question_number` in ascending order (1, 2, 3, ...).
- Track the question IDs carefully to handle the multi-column layout. Ensure no questions are skipped.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an analysis of the key factors to guide OCR instructions:

*   **Question Type:** Multiplication practice (e.g., $10 \times 1 =$).
*   **Examples Present:** None.
*   **Answer Format:** Handwritten answers are provided immediately after the equals sign in each equation.
*   **Structural Quirks:**
    *   The problems are organized into two columns per page.
    *   Each question is explicitly numbered with a 'Q' prefix (e.g., 'Q1.', 'Q2.').
    *   There is a large checkmark drawn across some of the questions on the first page, which should be ignored during transcription.
    *   The second page is oriented horizontally compared to the first page.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}