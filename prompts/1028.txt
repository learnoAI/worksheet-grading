You are a specialized OCR system designed to extract math worksheet data into a strict JSON format.

**TASK:**
Extract every question (Q1 to Q40) and its corresponding student answer from the provided images. The worksheet is split across two columns and images. You must extract and combine all data into a single, flat JSON array, sorted strictly ascending by `question_number` from 1 to 40.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a JSON array of objects using this exact structure. DO NOT use dynamic keys. DO NOT nest objects.
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**EXTRACTION & FORMATTING RULES (CRITICAL):**

1. **Pre-filled Examples (Q1, Q2, Q3, Q4 ONLY):**
   - The first four questions (Q1 through Q4) are examples with printed answers.
   - For these four items ONLY, include the printed answer in the `question` field and leave the `student_answer` field completely blank (`""`).
   - *Example for Q1:*
     `{"question_number": 1, "question": "Q1. 11 x 2 = 22", "student_answer": ""}`

2. **Student Questions (Q5 through Q40):**
   - For all remaining questions, the `question` string MUST stop exactly at the equals sign (`=`).
   - DO NOT append the student's handwritten answer to the `question` string. 
   - Any handwritten numbers following the equals sign must be extracted strictly into the `student_answer` field as a string. If blank, use `""`.
   - *Example for Q5:*
     `{"question_number": 5, "question": "Q5. 11 x 6 =", "student_answer": "66"}`

3. **Text Formatting:**
   - Always include the question label at the start (e.g., "Q1.", "Q2.", "Q30.").
   - Maintain consistent spacing around operators (e.g., "11 x 6 =").
   - Flatten vertical text into a single horizontal line.
   - NO newline characters in any strings.

4. **Handwriting Extraction:**
   - Distinguish carefully between **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**.

**PROCESSING ORDER:**
Regardless of the physical layout (left/right columns) or how the images are split, your final JSON output MUST be a single list ordered exactly from `question_number` 1 to `question_number` 40.

=== WORKSHEET CONTEXT NOTES ===
Here's a breakdown of the worksheet's key features to guide OCR instruction writing:

*   **Question Type:** Simple multiplication problems (multiplying by 11).
*   **Examples Present:** The first four questions (Q1-Q4) appear to be examples, as they include printed answers, whereas the rest require student input. They are formatted exactly like the other questions but with the answer already provided in print.
*   **Answer Format:** Students write the answer directly to the right of the equals sign for each equation. The first four questions have printed answers, so the `student_answer` field should be left blank for those.
*   **Structural Quirks:**
    *   The page is divided into two vertical columns of questions.
    *   Questions are numbered sequentially from top to bottom in the first column (Q1-Q10, then Q21-Q30) and then the second column (Q11-Q20, then Q31-Q40), though the image is split into two parts. The numbering follows "Q1.", "Q2.", etc.
    *   The first image contains questions 21-40, while the second image contains questions 1-20. The OCR should extract them and combine them into a single list, ordered sequentially from 1 to 40 based on the question numbers.
    *   There's a vertical separator line between the two columns.
    *   The first four questions in the second image have printed answers. In the target JSON, the entire equation (e.g., "Q1. 11 x 2 = 22") is treated as the question, with an empty string for the student answer. For the remaining questions, only the part up to the equals sign (e.g., "Q5. 11 x 6 =") is the question, and the handwritten number is the `student_answer`.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}