You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**WORKSHEET CONTEXT:**
- **Topic:** "What comes before" (Number sequence).
- **Format:** Questions are arranged in two parallel vertical columns. Each question starts with "Q" and a number, followed by a blank line (where the student writes), a comma, and a given number (e.g., "Q1. ____, 57").
- **Answer Format:** Fill-in-the-blank. The student's handwritten answer sits on the underline.
- **Examples:** There are no explicit examples; processing starts immediately with Q1.

**TASK:**
Extract every question and its corresponding student answer from the image provided, accurately traversing both columns to capture Q1 through Q40.

**STRICT JSON SCHEMA:**
You MUST wrap your output in a root object with the key `"results"` containing an array of exactly 40 question objects. Do NOT output a direct array. Do NOT use dynamic keys.

```json
{
  "results": [
    {
      "question_number": 1,
      "question": "Q1. , 57",
      "student_answer": "56"
    }
  ]
}
```

**FIELD EXTRACTION RULES:**
1. **`question_number` (Integer):** 
   - Extract ONLY the numerical value (e.g., `1`, `2`, `36`).
   - **DO NOT** include the "Q" prefix. 
   - MUST be an integer data type, not a string.
2. **`question` (String):**
   - Format exactly as `"Q[Num]. , [Given Number]"`.
   - Example: For the worksheet text "Q1. ____, 57", output `"Q1. , 57"`.
   - Flatten vertical text into a single horizontal string. 
   - Omit the underline `____`.
   - **NO** newline characters.
3. **`student_answer` (String):**
   - Extract exactly what the student wrote in pencil on the underline.
   - If the answer slot is blank, use an empty string `""`.
   - Always return the answer as a string (even if it contains letters).

**CRITICAL OCR & HANDWRITING GUIDELINES (READ CAREFULLY):**

- **DO NOT SOLVE THE MATH / DO NOT AUTO-CORRECT:** This is your most critical directive. Students make logical mistakes. Do NOT auto-correct their answers to logically fit the mathematical sequence. You must transcribe the raw visual ink.
  - *Case Study (Q10):* The question is `Q10. , 52`. The logically correct answer is 51, BUT the student visually wrote `57`. You MUST output `"57"`. Do not output `"51"`.
  - *Irregular Answers:* If the student wrote `16` or `12` where a larger number is expected (e.g., Q8, Q15, Q21), output exactly what is written (`"16"`, `"12"`).

- **TRANSCRIBE ALPHANUMERIC ERRORS "AS-IS":** Students sometimes write letters in numeric fields. 
  - *Case Study (Q36):* The student wrote `"2L"`. Transcribe it EXACTLY as `"2L"`. DO NOT force it into a number like `"24"` or `"81"`.

- **AMBIGUOUS CHARACTERS & KNOWN TROUBLE SPOTS:** Pay extremely close attention to sloppy handwriting. Distinguish carefully between:
  - **1 vs 7:** Look closely for horizontal crossbars or angled tops. (e.g., Q10 is `"57"`, not `"51"`).
  - **1 vs 6:** Sloppy curves can make a 6 look like a 1 or vice versa. (e.g., Q16 is `"62"`, DO NOT misread as `"12"`).
  - **4 vs L:** Look for closed angles vs open angles. (e.g., Q36 is `"2L"`, DO NOT misread as `"24"`).
  - **6 vs 8:** Check if loops are fully closed. (e.g., ensure a sloppy `"56"` in Q38 is not misread as `"58"`).
  - **5 vs 6** and **6 vs 0**.

**PROCESSING ORDER:**
- Process the layout accurately by tracking the Question numbers from 1 to 40.
- Ensure all 40 questions are accounted for.
- Sort the final `"results"` array strictly by `question_number` in ascending order (1, 2, 3 ... 40).

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Number sequence (identifying the preceding number).
- **Examples Present:** None explicitly labeled as examples.
- **Answer Format:** Fill-in-the-blank (student writes the number on an underlined space before a comma and a given number).
- **Structural Quirks:** The worksheet has a main title ("What comes before"), with questions organized into two parallel vertical columns. Each question is prefixed with 'Q' followed by a number (e.g., Q1., Q21.). The prompt format is `[Question Number] ____, [Given Number]`.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}