You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet images. The worksheet requires students to circle the biggest number in a set. 

**STRICT JSON SCHEMA:**
You MUST wrap your output in a root `"results"` object containing an array of question objects. 
Respond EXACTLY in this format:
```json
{
  "results": [
    {
      "question_number": 1,
      "question": "Q1. 7 , 2 , 30",
      "student_answer": "30"
    }
  ]
}
```

**FIELD-BY-FIELD FORMATTING RULES:**

1. **`question_number` (Integer):**
   - Must be an **integer**, not a string (e.g., `1`, `2`, `3`).
   - Extract only the numeric value from the question label. Strip away the "Q" and the period.

2. **`question` (String):**
   - Transcribe the text EXACTLY as it appears in the image.
   - **Include the label:** Must start with the label (e.g., "Q1.", "Q2.").
   - **Preserve exact whitespace:** You MUST include spaces before and after commas exactly as they appear (e.g., `"Q1. 7 , 2 , 30"`, NOT `"Q1. 7, 2, 30"`).
   - **CRITICAL ANTI-HALLUCINATION RULE:** DO NOT guess, infer, or fill in missing numbers. If a number is visibly missing or left blank in a sequence, leave it blank exactly as it appears on the page. For example, if a sequence ends abruptly (e.g., `"Q20. 1 , 13 ,"`), you MUST transcribe it EXACTLY as `"Q20. 1 , 13 ,"` without adding a final number. Do not hallucinate a '9' or any other digit.
   - Flatten vertical text into a single horizontal line. NO newline characters.

3. **`student_answer` (String):**
   - Extract the number that the student has drawn a circle around.
   - Return the answer as a string (e.g., `"30"`).
   - If no number is circled or the slot is entirely blank, use an empty string `""`.

**WORKSHEET CONTEXT, LAYOUT & ACCURACY RULES:**
- **Layout:** The questions are organized into two vertical columns. Read down the first column, then the second column.
- **Sorting:** Ensure the final JSON array is sorted by `question_number` in ascending order (1 through 40).
- **Ignore Extra Marks:** Ignore any handwritten grading marks at the top right of the page (e.g., "28/8 A1").
- **Digit Boundaries & Recognition:** Pay extra attention to the visual boundaries and spacing of numbers. Differentiate single digits from double digits carefully when handwriting or print quality is ambiguous (e.g., strictly verify whether a number is a single digit "6" rather than a double digit "18"). Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**. Look closely at the strokes before deciding.

=== WORKSHEET CONTEXT NOTES ===
Here is a breakdown of the worksheet's key features to assist with OCR instructions:

*   **Question Type:** Multiple-choice (identifying the largest number in a set).
*   **Examples Present:** None explicitly marked as examples, but the instructions say "Circle the biggest number".
*   **Answer Format:** Students answer by drawing a circle around the chosen number within the given sequence.
*   **Structural Quirks:**
    *   The questions are organized into two vertical columns per page.
    *   Each question is numbered with a "Q" prefix (e.g., "Q1.", "Q2.").
    *   The question text consists of a comma-separated list of three numbers.
    *   The student's intended answer is indicated by a hand-drawn circle around one of the numbers.
    *   There are handwritten markings at the top right (e.g., "28/8 A1") which are grading marks and not part of the questions.
    *   The second image appears to be a continuation of the first image (Questions 21-40).
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}