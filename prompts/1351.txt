You are a highly precise, specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every division question and its corresponding student answer (the final quotient) from the images provided. The worksheet contains 40 long division problems arranged in four vertical columns spanning across two pages.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a valid JSON object containing a single root key `"box_2d"`, which holds an array of objects representing the questions. 
DO NOT output an array directly at the root. DO NOT use dynamic keys. NO markdown blocks or commentary outside the JSON.

Use this exact structure:
```json
{
  "box_2d": [
    {
      "question_number": 1,
      "question": "Q1. 4 ) 4",
      "student_answer": "1"
    }
  ]
}
```

**FORMATTING RULES:**

1. **Question Field (`question`):**
    - **ABSOLUTE LITERAL TRANSCRIPTION:** You must transcribe the printed question label and text EXACTLY as it appears on the page. The worksheet contains intentional formatting inconsistencies that you MUST preserve.
    - **PRESERVE EXACT SPACING:** Do NOT remove spaces if they exist, and do NOT add them if they don't. 
        - Example: If the text is "Q1.", output `"Q1."` (no space). 
        - Example: If the text is "Q 6.", output `"Q 6."` (with space).
    - **NO AUTO-CORRECTION (CRITICAL):** Do NOT standardize the labels or add missing letters. 
        - **WARNING:** Look extremely closely at question 12. It explicitly omits the "Q" prefix. You MUST output exactly `"12. 4 ) 8"`. NEVER output `"Q12."` or `"Q 12."`.
    - Include the division format exactly as printed, ensuring correct spacing (e.g., `"4 ) 8"`).
    - Flatten vertical text into a single horizontal line with NO newline characters.

2. **Student Answer Field (`student_answer`):**
    - Extract **ONLY the final quotient** written in pencil ABOVE the division bracket.
    - **CRITICAL:** Do NOT include any step-by-step working out, subtraction steps, remainders, slashes, or zero/X marks written below the dividend (e.g., output `"1"`, NEVER `"1 / 4 \ - 4 / 0"`).
    - If the answer slot above the bracket is blank, use an empty string `""`.
    - Always return the answer as a string.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation for the quotient.
- Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
- If a number is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER:**
- Sort the final output by `question_number` in ascending order (1 to 40).
- Handle the four-column layout correctly by tracking the printed question numbers. Questions read down the columns or across, follow the logical numerical sequence of the labels. Multiple pages/images are a continuation of the exact same sequence.

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Division problems arranged in a long division format.
- **Examples Present:** None clearly marked as examples; all appear to be student exercises.
- **Answer Format:** The student writes the quotient above the division bracket and the subsequent subtraction steps below the dividend. The target JSON only captures the final quotient as the `student_answer`.
- **Structural Quirks:**
    - Arranged in four vertical columns.
    - Numbering format is typically 'Q' followed by a number (e.g., 'Q1.', 'Q 2.', 'Q 21.'), though some variations exist (e.g., '12.' instead of 'Q 12.').
    - The question format represents long division, e.g., "4 ) 4".
    - Handwriting is present for the answers and the steps of the long division.
    - The layout spans two pages (images), with questions continuing sequentially.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}