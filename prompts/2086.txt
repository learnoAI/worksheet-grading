You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question (1 through 40) and its corresponding student answer from the worksheet image. 

**JSON SCHEMA ENFORCEMENT:**
You must return a single JSON object with a root key `"questions"` containing an array of objects. 
Use exactly this structure:
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q 1. 401 x 300",
      "student_answer": ""
    }
  ]
}

**DO NOT** return a flat array. 
**DO NOT** use dynamic keys. 
**KEYS MUST BE:** `"question_number"`, `"question"`, `"student_answer"`.

**FIELD FORMATTING RULES:**

1. **`question_number` (Integer):**
   - Must be strictly a numeric integer (e.g., `1`, `13`, `21`, `40`).
   - **DO NOT** include prefixes or punctuation (No "Q1.", no "1.").

2. **`question` (String):**
   - You must flatten the vertical math problem into a single horizontal string.
   - You **MUST** prefix the string with the exact printed question label (e.g., "Q 1. ", "Q 13. ", "Q 21. ").
   - Format: `[Question Label] [Top Number] x [Bottom Number]` (Example: `"Q 21. 884 x 300"`).
   - **STRICT ALIGNMENT & ANTI-DUPLICATION:** Read each question completely independently. Visually trace the question number directly to its specific top number. Do NOT duplicate consecutive problems (e.g., do not copy Q14's text into Q13).
   - **KNOWN TRAPS:** Double-check the printed digits for the top number. Rely ONLY on the printed text. For example, Q13 is exactly `"Q 13. 193 x 300"`; do not misread it as 842 or duplicate Q14's "845 x 300".

3. **`student_answer` (String):**
   - Extract EXACTLY what the student handwrote in pencil below the line.
   - **STRICT ANTI-AUTOCORRECT (NO MATH LOGIC):** You MUST NOT use arithmetic logic, multiplication rules, or mathematical context to guess, fill in, or autocorrect the answers. Students make math errors! If a student wrote an incorrect digit (e.g., writing a '9' when the correct math answer would have a '2', such as writing "953500" for Q14), you MUST transcribe the handwritten '9'.
   - **VISUAL ACCURACY:** Look extremely closely at handwritten digits. Rely SOLELY on the visual strokes. Pay extreme attention to easily confused handwritten numbers: **2 vs 9**, **4 vs 9**, **7 vs 9**, **3 vs 5**, **3 vs 2**, **5 vs 6**, **1 vs 8**, and **6 vs 0**. 
   - **FAINT PRINT RULE (EXAMPLES):** The first five questions (Q1-Q5) contain faintly printed examples below the line instead of handwritten answers. You must treat these as blank/unanswered. Return an empty string `""`.
   - **COMPLETENESS CHECK:** Verify that every question has a corresponding `student_answer` mapped unless the space is completely physically blank. **Do not drop answers.** Pay special attention to Questions 21 through 25; answers DO exist there, do not falsely output an empty string.
   - If the answer slot has no student handwriting at all, use an empty string `""`.

**LAYOUT & EXTRACTION GUIDELINES:**
- The page is a 4-column grid. Track the question numbers sequentially from 1 to 40 to ensure no questions are skipped and the reading order remains intact.
- The question number (e.g., "Q 1.") is positioned to the left of the second row (the multiplier).
- Student handwriting often crosses grid lines or includes extra horizontal underlines. Ignore the drawn lines and focus only on the handwritten digits.
- **FINAL VERIFICATION:** Before outputting, perform a strict sequential check (1 through 40). Verify that no question text was duplicated from a neighboring problem and that all transcribed answers purely reflect the visual evidence on the page without mathematical autocorrection.
- Sort the final array by `question_number` in ascending order (1 to 40).
- Output ONLY valid JSON. Do not include markdown blocks like ```json, greetings, or explanations.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here are the key factors to help write better OCR instructions:

*   **Question Type:** Vertical multiplication problems (multiplying a 3-digit number by 300).
*   **Examples Present:** There are no clearly labeled examples. However, the first five questions (Q1-Q5) contain faintly printed answers below the line instead of handwritten ones. Crucially, the target JSON treats these as unanswered (empty strings for `student_answer`), so the OCR must distinguish between printed and handwritten text in the answer area.
*   **Answer Format:** Students handwrite their numerical answers directly below the printed horizontal line of each vertical problem. Note that students often draw an additional horizontal line under their written answers.
*   **Structural Quirks:**
    *   **Grid Layout:** The page is divided into a 4-column grid with vertical and horizontal separator lines. Student handwriting often crosses these grid lines.
    *   **Numbering Placement:** The question number (e.g., "Q 1.", "Q 21.") is positioned to the left of the second row (the multiplier).
    *   **Data Flattening:** The vertical structure of the math problem needs to be flattened into a single-line string for the `question` field in the JSON (Format: `[Question Number] [Top Number] x [Bottom Number]`, e.g., "Q 21. 884 x 300").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}