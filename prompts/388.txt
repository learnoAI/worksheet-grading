You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**WORKSHEET CONTEXT:**
- The worksheet focuses on finding the missing middle number in a sequence of three consecutive numbers ("What comes in between").
- The page uses a distinct two-column vertical layout.
- Questions are labeled with prefixes like "Q1.", "Q2.", etc.
- There are no pre-solved examples; all blanks contain handwritten student answers.

**TASK:**
Extract every question and its corresponding handwritten student answer from the image provided.

**STRICT JSON SCHEMA:**
You must output a single JSON object with a root key `"questions_and_answers"`. This key must contain an array of objects. Do NOT output a raw list/array at the root.

Example Structure:
{
  "questions_and_answers": [
    {
      "question_number": 1,
      "question": "Q1. 91 , __ , 93",
      "student_answer": "92"
    }
  ]
}

**FIELD EXTRACTION & FORMATTING RULES:**

1. **`question_number` (Integer)**
   - Extract the numeric part of the question label.
   - **MUST be an integer** (e.g., `1`, `2`, `31`).
   - DO NOT include the "Q" prefix in this field (e.g., use `1`, NOT `"Q1"`).

2. **`question` (String)**
   - Must include the full label at the start (e.g., "Q1.", "Q2.").
   - **CRITICAL PUNCTUATION & SPACING:** You must format the blanks and commas with exact spacing. Use a space before and after commas, and exactly two underscores for the blank.
   - Valid format: `"Q1. 91 , __ , 93"`
   - INVALID formats: `"Q1. 91, ___, 93"`, `"Q1. 91,__,93"`
   - Flatten the text into a single horizontal line. NO newline characters.

3. **`student_answer` (String)**
   - Extract exactly what the student wrote in pencil in the blank space.
   - Return the answer as a string (e.g., `"92"`).
   - If the answer slot is completely empty, use an empty string `""`.

**HANDWRITING & PROCESSING GUIDELINES:**
- Track Question IDs carefully to correctly navigate the two-column vertical layout.
- Sort the final output array strictly by `question_number` ascending (1, 2, 3, etc.).
- Look closely at digit formation (e.g., distinguish 9 vs 8, 6 vs 0, 1 vs 7 vs 4). If a number is messy, select the most likely digit based on visual stroke evidence.
- Output ONLY valid JSON. Do not include markdown formatting like ```json or any conversational text.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is the analysis of the key factors:

*   **Question Type:** Number sequence. Specifically, finding the missing middle number in a sequence of three consecutive numbers (as indicated by the "What comes in between" header).
*   **Examples Present:** None. There are no pre-solved examples provided on the pages; all blanks contain handwritten student answers.
*   **Answer Format:** Fill in the blanks. The student writes their numerical answer on a designated line situated between two printed numbers and commas.
*   **Structural Quirks:** The worksheet uses a distinct two-column vertical layout. Each question is numbered using a specific prefix format (e.g., "Q1.", "Q2.", "Q31.").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}