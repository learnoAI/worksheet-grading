You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every multiplication question and its corresponding student answer from the image provided. The problems are arranged vertically in a grid with four columns. The numbering goes down each column sequentially before moving to the next.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a JSON object containing a single root key `"questions"`. The value of `"questions"` must be an array of objects.
Each object in the array MUST have exactly these three keys: `"question_number"`, `"question"`, and `"student_answer"`.

Example Structure:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 27 x 6",
      "student_answer": "162"
    }
  ]
}
```

**FORMATTING RULES:**
1. **question_number** (Integer):
   - MUST be an integer representing the question number.
   - Extract ONLY the number. Strip out any "Q" prefixes or periods (e.g., return `1`, NOT `"Q1."`).
2. **question** (String):
   - Must flatten the vertical multiplication problem into a single horizontal string.
   - Must include the original label prefix (e.g., "Q1.", "Q2.") at the start.
   - Format exactly like: `"Q1. 27 x 6"`
   - **NO** newline characters in the string.
3. **student_answer** (String):
   - Extract exactly what the student wrote (often in blue pen/pencil) in the blank space below the horizontal line.
   - Return the answer as a string (e.g., `"162"`).
   - If the answer slot is blank, use an empty string `""`.

**EXTRACTION & LAYOUT GUIDELINES:**
- **Processing Order:** Read down each column vertically, tracking the Question IDs. Sort the final output array strictly by `question_number` in ascending order (1 through 40).
- **Ignore Teacher Marks:** Do NOT transcribe red checkmarks, circles, or teacher grading marks.
- **Digit Recognition:** Look closely at digit formation (distinguish 9 vs 8, 6 vs 0, 1 vs 7 vs 4). If a number is messy, select the most likely digit based on visual stroke evidence.
- Ensure the JSON is properly formatted and valid.

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Multiplication problems arranged vertically.
- **Examples Present:** None clearly marked as examples; however, the first few rows (Q1-Q5) appear to be pre-filled or answered in a different style, possibly serving as initial practice or examples before the student filled in the rest in blue pen.
- **Answer Format:** Blank space below a horizontal line under each multiplication problem, where the student writes the calculated product.
- **Structural Quirks:** The problems are arranged in a grid with four columns. The questions are numbered sequentially (Q1, Q2, etc.) but the numbering goes down each column before moving to the next column. The questions should be written in a linear format (e.g., "Q1. 27 x 6") in the JSON output, combining the question number and the numbers to be multiplied. Teacher's grading marks (red checkmarks and circles) are present and should be ignored.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}