You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. On this worksheet, the student indicates their answer by drawing a hand-written circle around one of the numbers.

**STRICT JSON SCHEMA:**
You must output a single JSON object with the root key `"worksheet_data"` containing an array of exactly 40 objects. Do not return a raw array.
```json
{
  "worksheet_data": [
    {
      "question_number": <integer>,
      "question": "<string>",
      "student_answer": "<string>"
    }
  ]
}
```

**EXTRACTION & FORMATTING RULES:**

1.  **question_number (Integer):**
    - Extract the question number as a strict **integer** (e.g., `1`, `2`, `36`). 
    - Do NOT include "Q" or the period in this field (e.g., do not output `"Q1."`).

2.  **question (String):**
    - Transcribe the printed text exactly as it appears left-to-right.
    - You MUST include the full label prefix at the start (e.g., "Q1.", "Q2.").
    - **CRITICAL SPACING:** You MUST preserve the exact spaces before and after commas. Format it exactly like this: `"Q1. 73 , 75 , 88 , 100"`.
    - **EXACTLY 4 NUMBERS:** There are exactly 4 numbers listed per question. Do not duplicate numbers or hallucinate extra digits.
    - **DIGIT RECOGNITION (7 vs 9):** Pay extreme attention to distinguishing '7' from '9'. Do not mistake a '7' for a '9' (e.g., in Q2, verify the second number is "72", not "92"). Check every single digit carefully. Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
    - **NO** newline characters in the string.

3.  **student_answer (String) - CRITICAL RULES:**
    - Look ONLY for a **physically drawn hand-written circle** around one of the numbers. The circled number is the student's answer.
    - Extract exactly the number that is circled as a string (e.g., `"97"`).
    - **MISSING DATA / BLANKS:** If a student answer is missing or left blank on the worksheet (i.e., NO circle is drawn around any number, such as in Q36), you MUST return an empty string `""`.
    - **DO NOT SOLVE:** Do not attempt to calculate, guess, or solve the math problem (finding the maximum value) yourself. Pure transcription only.
    - **IGNORE NOISE:** There are large stray pen marks/lines crossing through multiple questions on the page. Completely ignore these large strokes and scribbles. Focus solely on individual, closed circles around specific numbers.

**PROCESSING ORDER:**
- The worksheet is laid out in multiple columns (e.g., Q1-10 on the left, Q11-20 on the right). Read the layout correctly to capture all 40 questions.
- Sort the final output strictly by `question_number` in ascending order (1 through 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an analysis of the key factors to help write better OCR instructions:

*   **1. Question Type:**
    *   Number comparison (identifying the maximum value in a given set of four numbers).

*   **2. Examples Present:**
    *   None. The worksheet begins directly with question Q1 without any pre-solved examples demonstrating the task.

*   **3. Answer Format:**
    *   Circling. The student answers by drawing a handwritten circle around one of the numbers in the list.
    *   The target JSON extracts the circled number as a string for the `student_answer`. If no number is circled (as seen in Q36), the `student_answer` field is left as an empty string `""`.

*   **4. Structural Quirks:**
    *   **Column Layout:** The questions are arranged in a two-column format on each page (e.g., Q1-Q10 on the left, Q11-Q20 on the right).
    *   **Question Formatting:** Each question has a specific prefix format ("Q" followed by the number and a period, e.g., "Q1."). The JSON requires capturing this full prefix, the numbers, and the comma separators exactly as they appear (e.g., "Q1. 73 , 75 , 88 , 100").
    *   **Stray Marks/Noise:** There are large, stray pen strokes crossing through multiple questions (e.g., a line crossing through Q17, Q8 on the first page, and a checkmark-like scribble near Q29 on the second page). The OCR must be instructed to ignore these large stray marks and focus only on the circles indicating the specific answers.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}