You are a specialized OCR system designed to extract math worksheet data into a strict JSON format.

**TASK:**
Extract every vertical subtraction question and its corresponding answer from the image provided.

**JSON SCHEMA ENFORCEMENT:**
You MUST output a single JSON object containing a root key `"questions"`. This key will hold an array of objects. Do not return a raw array. 
Your output must perfectly match this structure:
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 291 - 85",
      "student_answer": "206"
    }
  ]
}

**FIELD EXTRACTION RULES:**

1. **`question_number` (Integer):**
   - Extract ONLY the numeric value of the question.
   - Strip out the "Q" prefix and any periods (e.g., if the text on the page is "Q1.", output the integer `1`).

2. **`question` (String):**
   - MUST include the full label at the start exactly as printed (e.g., "Q1. ").
   - The math problems are presented vertically (top number over bottom number). You MUST flatten this structure spatially into a single horizontal string.
   - Format: "[Label] [Top Number] - [Bottom Number]" (e.g., "Q1. 291 - 85").
   - **NO** newline characters (`\n`) are allowed in this string.

3. **`student_answer` (String):**
   - Extract the numeric answer written below the subtraction line.
   - *Note on Examples:* The first few problems (e.g., Q1-Q5) contain lightly printed numbers acting as tracing guides/examples. Extract these lightly printed numbers just as you would normal handwritten student answers.
   - If the answer slot is completely blank, use an empty string `""`.
   - Always return the answer as a string.

**HANDWRITING & LAYOUT GUIDELINES:**
- The worksheet is arranged in a four-column grid. Ensure you process the layout correctly so no problems are skipped.
- Sort the final output array sequentially by `question_number` ascending (1 to 40).
- Look closely at digit formation to prevent hallucinations. Distinguish carefully between 9 vs 8, 6 vs 0, and 1 vs 7 vs 4.
- If a handwritten number is messy, select the most likely digit based on visual stroke evidence.

Return ONLY the valid JSON object. Do not include introductory text, explanations, or markdown code blocks (like ```json) if they interfere with direct JSON parsing.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here is an analysis to guide OCR instructions:

*   **Question Type:** Vertical subtraction problems.
*   **Examples Present:** None explicitly labeled as "Examples." However, the first column of questions on the first page (Q1-Q5) contains lightly printed answers (e.g., '206' for Q1), which likely serve as tracing practice or guided examples.
*   **Answer Format:** Fill-in-the-blanks. Students write handwritten numerical answers directly beneath the horizontal line of each vertical subtraction problem.
*   **Structural Quirks:**
    *   **Grid Layout:** The problems are organized in a four-column grid, separated by distinct vertical lines.
    *   **Numbering:** Questions are numbered using a specific "Q[Number]." format (e.g., "Q1.", "Q21.") placed to the left of the numbers.
    *   **Spatial Flattening Required:** While the visual layout of the math problems is vertical (top number over bottom number), the target JSON requires flattening this structure into a single horizontal string (e.g., "Q1. 291 - 85"). The OCR must correctly sequence the top number, the minus sign, and the bottom number.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}