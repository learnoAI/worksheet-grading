You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. The worksheet contains vertically arranged multiplication problems sequentially numbered from 1 to 40 across the pages.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a JSON array of objects. 
**DO NOT** use dynamic keys.
**DO NOT** nest the objects.
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES:**

1. **Question Number (`question_number`):**
   - MUST be a pure **integer** (e.g., `1`, `2`, `3`).
   - Do NOT include any text prefixes or quotes (e.g., use `1`, NOT `"Q1"` or `"1"`).

2. **Question Field (`question`):**
   - Must begin with the label formatted exactly as "Q[number]." with **NO SPACE** between the 'Q' and the number (e.g., `"Q1."`, NOT `"Q 1."`).
   - Must flatten the vertically arranged math problem into a single horizontal string (e.g., `"Q1. 40 x 4"`).
   - Ensure there is a space after the period, and spaces around the "x".
   - **NO** newline characters in the string.

3. **Student Answer Field (`student_answer`):**
   - Extract exactly what the student wrote in pencil below the line.
   - If the answer appears overwritten or corrected, extract the final intended written number.
   - If the answer slot is blank, use an empty string `""`.
   - Return the answer as a **string** (e.g., `"160"`).

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation (e.g., distinguish 9 vs 8, 6 vs 0, 1 vs 7 vs 4).
- If a number is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER:**
- Process all rows and columns across the image(s) to capture all 40 questions.
- Sort the final JSON array strictly by `question_number` in ascending order (1 to 40).

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Multiplication problems arranged vertically.
- **Examples Present:** None clearly marked as examples; all appear to be completed problems.
- **Answer Format:** Student answers are handwritten below the line in each problem.
- **Structural Quirks:** Problems are organized in columns and rows. Each problem has a "Q[number]." label (e.g., Q 1., Q 2.). The target JSON formats the question as a horizontal string combining the label and the numbers (e.g., "Q1. 40 x 4"). Some answers look like they might have been corrected or overwritten (e.g., Q6, Q8), but the OCR should extract the final written number. The JSON expects all questions to be numbered sequentially from 1 to 40 across the two pages.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}