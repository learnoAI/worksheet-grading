You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**WORKSHEET CONTEXT:**
- The worksheet asks students to "Circle the biggest number" from a given set of four numbers.
- Questions are arranged sequentially from Q1 to Q40 in two vertical columns.
- Each question has a label (e.g., Q1.) followed by exactly 4 numbers separated by commas.
- The student's answer is indicated by a pencil circle drawn around one of the numbers.
- Ignore all headers (Name, Token Number, Number Order, Marks) and footers/watermarks (Saarthi EDUCATION, timestamps like 2025.07.05).

**TASK:**
Extract every question and its corresponding student answer from the image provided.

**JSON SCHEMA ENFORCEMENT:**
Respond in the following JSON format, providing an array of objects for all questions:
{format_instructions}

**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects.
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**DATA EXTRACTION & FORMATTING RULES:**
1.  **"question_number" Field:**
    - Must be strictly an **INTEGER** (e.g., `1`, `2`, `3`).
    - **DO NOT** include the "Q" prefix or the period. (e.g., for "Q1.", output `1`).
2.  **"question" Field:**
    - Must include the full question text as printed, including the original label.
    - Start with the exact label (e.g., "Q1.", "Q2.").
    - Follow immediately with the exactly **FOUR** comma-separated numbers printed on that line.
    - Example format: `"Q1. 21 , 23 , 26 , 27"`
    - **ANTI-HALLUCINATION WARNING:** Transcribe strictly what is printed. Do NOT add, prepend, or invent extra numbers that are not in the sequence. Each sequence contains exactly 4 numbers.
    - Must flatten text into a single horizontal string. **NO** newline characters.
3.  **"student_answer" Field:**
    - Extract the specific number that the student circled in pencil.
    - Return the answer as a **STRING** (e.g., `"27"`).
    - If no number is circled or the question is unattempted, use an empty string `""`.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation for the circled numbers.
- Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
- If a number or circle is messy, select the exact printed number that the circle is primarily enclosing.

**PROCESSING ORDER:**
- Parse line-by-line down the columns.
- Ensure all 40 questions are captured.
- Sort the final output strictly by `question_number` ascending (1 through 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here is an analysis of the key factors to guide OCR instructions:

*   **Question Type:** Identifying the maximum value from a given set of four numbers. The instructions at the top say "Circle the biggest number" and its Hindi translation "सबसे बड़े नंबर पर गोला लगाओ".
*   **Examples Present:** There are no explicitly marked solved examples. The student has attempted all visible questions.
*   **Answer Format:** The student marks their answer by drawing a circle (often roughly drawn in pencil) around the number they identify as the largest in each row. The target JSON captures this circled number as the `student_answer`.
*   **Structural Quirks:**
    *   The page is divided into two vertical columns of questions.
    *   Questions are numbered sequentially with a 'Q' prefix (e.g., Q1., Q2., ..., Q40.).
    *   Each question consists of the question identifier followed by four comma-separated numbers on a single line.
    *   The layout is highly structured and tabular, making it straightforward to parse line-by-line within columns.
    *   There is header information at the top (Name, Token Number, Number Order, Marks) that is excluded from the final JSON structure, focusing only on the questions.
    *   There is a watermark/logo at the bottom right "Saarthi EDUCATION" and a timestamp "2025.07.05 11:49" or "2025.07.05 11:50" which are also ignored in the JSON output.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}