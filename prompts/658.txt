You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. 

**JSON SCHEMA ENFORCEMENT:**
You MUST output a single JSON object containing a top-level `"questions"` array. 
Do NOT output a top-level array. Do NOT use dynamic keys.
Follow this exact structure and data type format:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 3 x 8 = 24",
      "student_answer": "24"
    }
  ]
}
```

**FIELD DEFINITIONS & FORMATTING RULES:**
1. **`question_number` (Integer):**
   - Extract the numeric value only (e.g., `1`, `2`, `3`). 
   - DO NOT include the "Q" or the period (e.g., write `1`, NOT `"Q1"`).

2. **`question` (String):**
   - Must include the full label (e.g., "Q1.", "Q2."), the equation, **AND the student's answer after the equals sign.**
   - Example: If the paper says "Q1. 3 x 8 =" and the student wrote "24", the output MUST be `"Q1. 3 x 8 = 24"`.
   - Flatten vertical text into a single horizontal line. NO newline characters.

3. **`student_answer` (String):**
   - Extract exactly what the student wrote in pencil next to the equals sign.
   - If the answer slot is blank, use an empty string `""`.

**CRITICAL RULES FOR ACCURACY (DO NOT AUTOCORRECT):**
- **NEVER CORRECT MATH ERRORS:** Transcribe the student's answers exactly as written, even if the math is blatantly incorrect. 
  - *Example:* If the student wrote "16" for 3 x 5, you MUST output `"student_answer": "16"` and `"question": "Q9. 3 x 5 = 16"`.
  - *Example:* If the student wrote "218" for 3 x 6, output `"218"`.
  - *Example:* If the student wrote "6 4" for 3 x 3, output `"6 4"`.
- **IGNORE GRADING & WATERMARKS:** Do NOT extract the red grading ink (like "23/8"), the large red checkmarks, or the watermarks at the bottom of the page (e.g., "POCO M5", "23/08/2025 11:40", "Saarthi EDUCATION").

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation (e.g., distinguish 9 vs 8, 6 vs 0, 1 vs 7 vs 4).
- Process all questions and sort the final output by `question_number` ascending (1 through 40). 
- Handle multiple columns correctly by following the printed Question IDs.

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Multiplication equations.
- **Examples Present:** None.
- **Answer Format:** The student writes the answer to the right of the equals sign for each equation.
- **Structural Quirks:** The worksheet consists of two images, each containing two columns of numbered questions formatted as 'Q[Number].'. There's handwritten text mixed with printed text, including a grade written in red ink ("23/8" or similar) and a red checkmark extending across two columns in both images. There are also watermarks/text at the bottom ("POCO M5", "23/08/2025 11:40", "Saarthi EDUCATION").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}