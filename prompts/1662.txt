You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided.

**JSON SCHEMA ENFORCEMENT:**
Respond in the following JSON format, providing a list of all 40 questions and their answers:
[
  {
    "question_number": 1,
    "question": "Q1. 714 + 920",
    "student_answer": "1634"
  }
]

**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects.
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES:**
1.  **Question Field:**
    - Must include the label (e.g., "Q1.", "Q2.") at the start.
    - Must flatten the vertically stacked addition problem into a single horizontal string (e.g., "Q1. 714 + 920").
    - Ignore any small carry-over numbers the student may have written above the top numbers.
    - **NO** newline characters in the string.
2.  **Student Answer Field:**
    - Extract exactly what the student wrote in pencil below the horizontal line.
    - If the answer slot is blank, use an empty string `""`.
    - Return the answer as a string.

**CRITICAL TRANSCRIBING INSTRUCTIONS - NO AUTO-CORRECTION & IGNORING STRAY MARKS:**
- **DO NOT attempt to solve the equations.** 
- **DO NOT auto-correct mathematically incorrect answers.** 
- Many student answers contain arithmetic mistakes, extra digits, or misplaced digits. You MUST transcribe the literal numbers the student wrote as their final answer, regardless of whether the math is correct or entirely wrong.
- **IGNORE CROSSED-OUT TEXT AND SMUDGES:** Only extract the final intended written answers. Strictly ignore crossed-out text, smudges, erasures, or stray pencil marks preceding or surrounding the final answer (e.g., do not mistakenly transcribe a smudge or scribble as a "0" or "6").

**HANDWRITING EXTRACTION GUIDELINES:**
- Pay close attention to messy, cursive, or overlapping handwriting. 
- Extraneous strokes might look like extra digits (e.g., an accidental '1' written at the front of an answer) – verify if it is part of the final answer or a stray mark.
- Look closely at digit formation and carefully differentiate between visually similar digits. Take a second look to distinguish:
  - **2** vs **3** (Carefully trace the loops and curves)
  - **1** vs **7** vs **4**
  - **0** vs **6**
  - **8** vs **9**
- If a number is messy, select the most likely digit based strictly on visual stroke evidence.

**PROCESSING ORDER:**
- Read through the multi-column, multi-row grid systematically.
- Ensure all 40 questions are captured.
- Sort the final JSON array by `question_number` in ascending order (1 to 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here is an analysis of the key factors to guide OCR instructions:

*   **Question Type:** Vertical addition of 3-digit numbers.
*   **Examples Present:** None. There are no pre-solved examples provided by the worksheet; all answers are filled in by the student.
*   **Answer Format:** Handwritten numbers written directly below the horizontal line of each vertical addition problem. (Note: Students have written small carry-over numbers above the problems, which are correctly ignored in the target JSON).
*   **Structural Quirks:**
    *   **Grid Layout:** Questions are organized in a dense multi-column, multi-row grid.
    *   **Numbering Style:** Questions are prefixed with "Q" followed by the number and a period (e.g., "Q1.", "Q2.").
    *   **Format Conversion:** While the visual layout is stacked vertically, the target JSON requires transcribing the question into a horizontal equation format (e.g., converting stacked 714 and +920 into "Q1. 714 + 920").
    *   **Separators:** A horizontal line consistently separates the problem from the student's answer area.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}