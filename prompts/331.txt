You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every column (question) and its corresponding repetitive student handwritten answers from the image provided.

**WORKSHEET LAYOUT & EXTRACTION RULES:**
1. **Vertical Columns:** The worksheet consists of vertical columns, where each column is a single copy-work question.
2. **Question Field (Label + Header):** 
   - Locate the floating question label to the left of each column (e.g., "Q1.", "Q2.", "Q6.").
   - Locate the printed number in the top box of that column (e.g., "71", "72").
   - Combine these to form the `"question"` string: "[Label] [Printed Number]" (e.g., "Q1. 71").
3. **Student Answer Field (Vertical to Horizontal):** 
   - The student has written answers in stacked boxes down each column.
   - Extract all the student's handwritten numbers in a given column.
   - Flatten this vertical stack into a SINGLE horizontal string, separating each written number with a single space (e.g., "71 71 71 71 71 71 71 71").
   - Extract exactly what the student wrote in pencil. If blank, use "".
4. **Interference:** Completely ignore any large red grading lines or marks drawn diagonally across the page. Focus only on the printed text and pencil handwriting.

**JSON SCHEMA ENFORCEMENT:**
Your final output MUST be wrapped in a JSON object with a single key named `"questions"`, which contains the array of extracted items. DO NOT output a raw list/array at the root level.

Use exactly this format:
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 71",
      "student_answer": "71 71 71 71 71 71 71 71"
    }
  ]
}

**FIELD DEFINITIONS:**
- `"question_number"`: The integer number of the question (e.g., 1, 2, 3).
- `"question"`: The combined label and printed top number.
- `"student_answer"`: The flattened string of the student's vertical handwritten responses, separated by spaces.

**PROCESSING ORDER:**
- Process columns from left to right.
- Sort the final `"questions"` array by `question_number` in ascending order.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is the analysis to improve OCR instructions:

*   **Question Type:** Handwriting practice / Number copying (repetitive sequence writing).
*   **Examples Present:** Yes, the top box of every vertical column acts as the example/prompt (the printed number to be copied).
*   **Answer Format:** Students write their answers in vertically stacked boxes. However, the target JSON requires flattening these vertical student inputs into a single horizontal string, separated by spaces.
*   **Structural Quirks:**
    *   **Vertical Layout:** Unlike standard left-to-right questions, these are formatted in vertical columns.
    *   **Floating Question Labels:** The question markers (e.g., "Q 1.", "Q 6.") are placed somewhat awkwardly in the margins to the left of each column, halfway down the page, rather than at the top.
    *   **Interference:** A large red grading line spans diagonally across the student answers, which the OCR needs to ignore.
    *   **Combined 'Question' Field:** The target JSON merges the floating question label (e.g., "Q1.") with the column header (e.g., "71") to form the full `question` string ("Q1. 71").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}