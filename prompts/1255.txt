You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided.

**JSON SCHEMA ENFORCEMENT:**
Respond in the following JSON format, providing a list of all 40 questions and their answers:
{format_instructions}

**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects.
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING & EXTRACTION RULES:**

1. **`question_number` Field:**
   - MUST be strictly an **integer** (e.g., `1`, `2`, `3`).
   - DO NOT include "Q" or "." in this field.

2. **`question` Field:**
   - Must reconstruct the full horizontal equation and include the "Q" label at the start.
   - Example format: `"Q2. 13 x 11"`
   - **Context Clue / Hallucination Prevention:** The worksheet is structured in vertical columns. The first number in the multiplication is a header for the entire column. 
     - Q1-Q5 are all `13 x ...`
     - Q6-Q10 are all `23 x ...`
     - Q11-Q15 are all `33 x ...`
     - Q16-Q20 are all `43 x ...`
     - Q21-Q25 are all `53 x ...`
     - Q26-Q30 are all `63 x ...`
     - Q31-Q35 are all `73 x ...`
     - Q36-Q40 are all `83 x ...`
   - Use this pattern to avoid transcription errors. **NO** newline characters in the string.

3. **`student_answer` Field:**
   - Extract **ONLY the final numerical answer** (the final sum at the bottom).
   - **CRITICAL:** Ignore any scratchpad work, intermediate partial products, column addition, or multi-line math. (e.g., if the work shows "13\n130\n----\n143", you must ONLY extract `"143"`).
   - Return the answer as a string.
   - **EXAMPLES:** The first question in each column (Q1, Q6, Q11, Q16, Q21, Q26, Q31, Q36) is a fully solved example. Because the final answer is pre-printed and NOT written by the student, the `student_answer` for these MUST be an empty string `""`.
   - If a student left any other final answer slot blank, use an empty string `""`.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation.
- Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
- If a number is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER:**
- Sort the output by `question_number` ascending (from 1 to 40).
- Handle multiple columns correctly by tracking the Question IDs.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON output, here is the educational analysis:

*   **Question Type:** Multiplication problems (2-digit by 2-digit).
*   **Examples Present:** Yes, the first question in each column (Q1, Q6, Q11, Q16, Q21, Q26, Q31, Q36) serves as a fully solved example showing the step-by-step partial products and final sum. The final answers for these examples are left blank in the target JSON as they are pre-printed.
*   **Answer Format:** Students write their answers below the provided lines, showing their work (partial products) and the final sum at the bottom. The target JSON captures only the final sum as the `student_answer`.
*   **Structural Quirks:**
    *   The page is divided into vertical columns.
    *   Each question starts with an identifier like "Q1.", "Q2.", etc.
    *   The multiplication problem is presented vertically in the example, but subsequent questions just list the numbers horizontally next to the 'Q' identifier (e.g., "13 x 11"), expecting the student to set it up. However, the pre-printed text above the student's work often just shows the first number (e.g., '13') to act as a header for that set.
    *   The `question` field in the JSON reconstructs the full equation string in a single line (e.g., "Q2. 13 x 11").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}