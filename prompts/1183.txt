You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every math problem and its corresponding handwritten student answer from the provided multi-column worksheet image. 

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a JSON object containing a single root key `"questions_and_answers"`, which holds a list of the extracted items. **DO NOT** return a top-level array.

Use exactly this structure:
```json
{
  "questions_and_answers": [
    {
      "question_number": 1,
      "question": "Q 1. 11 x 8",
      "student_answer": "88"
    },
    ...
  ]
}
```

**DO NOT** use dynamic keys. **DO NOT** nest the objects further.
Keys inside the array objects MUST strictly be: `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES:**
1. **Root Object:** You must wrap the array in the `{"questions_and_answers": [...]}` object.
2. **Question Field (`question`):**
   - **CRITICAL SPACING:** You MUST include a space between the letter 'Q' and the question number. Format it exactly as: `"Q 1. "`, `"Q 2. "`, `"Q 15. "`, etc. (Do NOT output "Q1.").
   - **Flattening:** The math problems are printed vertically (one number on top of the other). You must flatten this into a single horizontal string using the format: `"Q [number]. [top_number] x [bottom_number]"`.
   - Example: If the problem is 'Q 15.' with '32' stacked above 'x 8', the output MUST be `"Q 15. 32 x 8"`.
   - **NO** newline characters in the string.
3. **Student Answer Field (`student_answer`):**
   - Extract the handwritten numeric answer written directly underneath the horizontal line.
   - Return the answer as a string (e.g., `"256"`).
   - If the answer slot is completely blank, use an empty string `""`.

**STRICT TRANSCRIPTION & ANTI-CALCULATION RULES:**
- **DO NOT CALCULATE OR AUTOCORRECT THE MATH:** You are evaluating student work, so capture mistakes exactly as they appear. Transcribe the handwritten answer exactly what is written on the page, even if it is mathematically incorrect. 
  - *Example:* If the problem is 53 x 8 and the student wrote "427", you MUST output `"427"`. Do NOT correct it to "424".
- Look very closely at digit formation to distinguish and prevent hallucinations:
  - **3** vs **5** (e.g., do not misread 32 as 57)
  - **2** vs **7**
  - **9** vs **8**
  - **6** vs **0**
  - **1** vs **7** vs **4**
- If a number is messy, select the most likely digit based strictly on visual stroke evidence.

**GRID NAVIGATION & SPATIAL ALIGNMENT:**
- The page contains a dense multi-column grid (4 columns). 
- **Prevent Shifted Text:** Read systematically (column-by-column or row-by-row) to ensure you map the correct operands and answer to the correct question number. 
- Do NOT skip equations or mix up numbers from adjacent problems. Locate the explicit "Q [number]." marker, then strictly bind it to the vertical numbers immediately to its right and the handwritten answer immediately below it.
- Ensure the final JSON array contains exactly all 40 questions, sorted sequentially by `question_number` in ascending order (from 1 to 40).

=== WORKSHEET CONTEXT NOTES ===
Here is an analysis of the worksheet and its target output to help formulate OCR instructions:

*   **Question Type:** Multiplication (specifically, multiplying 2-digit numbers by 8).
*   **Examples Present:** None. There are no pre-solved examples provided on the worksheet.
*   **Answer Format:** Handwritten numeric answers written directly underneath the horizontal line of each vertical math problem.
*   **Structural Quirks:**
    *   **Vertical Math Layout:** The problems are presented vertically (operands stacked on top of each other with the 'x' symbol to the left).
    *   **Formatting Transformation:** The target JSON requires flattening this vertical visual arrangement into a single, horizontal text string (e.g., the stacked "11", "x 8" must be transcribed as "11 x 8").
    *   **Grid Layout:** The questions are arranged in a dense, multi-column grid (4 columns per page).
    *   **Numbering:** Questions are explicitly numbered using the prefix 'Q' followed by a period (e.g., "Q 1.", "Q 21.").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}