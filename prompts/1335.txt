You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided, strictly adhering to the JSON schema, formatting rules, and reading order below.

**WORKSHEET CONTEXT & LAYOUT:**
- **Columns:** The worksheet has two vertical columns. Read the entire left column top-to-bottom first, then the right column top-to-bottom.
- **Exclusions:** Ignore the main bilingual instructions ("Divide the following." / "भाग करो.") and any extraneous handwritten dates, annotations, or grading marks near the top center of the page.

**JSON SCHEMA ENFORCEMENT:**
You MUST wrap your output in a root `"results"` object containing an array of question objects.
Do NOT use dynamic keys. Do NOT nest objects further. 
Return exactly this format:
```json
{
  "results": [
    {
      "question_number": 1,
      "question": "Q 1. 46 ÷ 9 =",
      "student_answer": "5 R 1"
    }
  ]
}
```

**FIELD-SPECIFIC FORMATTING RULES:**

1. **`question_number` (Integer):**
   - MUST be strictly typed as an **integer** (e.g., `1`, `2`, `15`). 
   - DO NOT include the "Q" or "." here (e.g., NOT `"Q1."`).

2. **`question` (String):**
   - Must flatten vertical text into a single horizontal line.
   - You MUST format the prefix to include a space after the 'Q': e.g., `"Q 1."`, `"Q 15."`.
   - Include the full equation and the equals sign. Example: `"Q 1. 46 ÷ 9 ="`
   - Use the standard division symbol (`÷`).
   - **NO** newline characters in the string.

3. **`student_answer` (String):**
   - Extract exactly what the student wrote in pencil in the open space after the `=`.
   - The required format includes the quotient, a capital 'R', and the remainder (e.g., `"5 R 1"`).
   - If the answer slot is completely blank/unanswered, use an empty string `""`.

**HANDWRITING EXTRACTION GUIDELINES:**
- Pay very close attention to handwritten digits.
- **CRITICAL:** Distinguish carefully between **'2'** and **'7'**. The student's '2' may look similar to a '7', so look closely at the stroke evidence.
- Also correctly distinguish **9** vs **8**, and **6** vs **0**.
- If a number is messy, select the most likely digit based on visual stroke evidence and the mathematical context of the division problem.

**PROCESSING ORDER:**
- Sort the final output strictly by `question_number` in ascending order (1 to 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an educational analysis of the key factors to optimize OCR instructions:

*   **Question Type:** Standard arithmetic division equations that result in a quotient and a remainder.
*   **Examples Present:** There are no explicitly labeled solved examples provided on the page. 
*   **Answer Format:** Students write their answers in the open space directly following the equals sign (`=`). The required format includes the quotient, the capital letter 'R', and the remainder (e.g., "5 R 1"). Unanswered questions are left completely blank.
*   **Structural Quirks:**
    *   **Two-Column Layout:** The questions are arranged in two vertical columns per page, divided by a thin central vertical line. The reading order should proceed down the left column first, then the right column.
    *   **Numbering Format:** Every question uses a specific alphanumeric prefix format: "Q [number]." (e.g., "Q 1.", "Q 15.").
    *   **Bilingual Text:** The primary instruction ("Divide the following.") is accompanied by its Hindi translation ("भाग करो.").
    *   **Extraneous Handwriting:** There are handwritten annotations (likely a date or grading mark) near the top center of the first page that should be ignored by the main parsing logic.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}