You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided.

**STRICT JSON SCHEMA ENFORCEMENT:**
Your response MUST be a JSON object containing a single root key called `"extraction_result"`. This key must hold the array of extracted items. 
**DO NOT** return a raw array. You must strictly follow this template:

{
  "extraction_result": [
    {
      "question_number": 1,
      "question": "Q1. 5 x 1 =",
      "student_answer": "5"
    }
  ]
}

**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects further.
**KEYS MUST EXACTLY BE:** `"question_number"` (integer), `"question"` (string), `"student_answer"` (string).

**FORMATTING RULES:**
1.  **Question Field:**
    - Must include the sequential label (e.g., "Q1.", "Q2.", etc.) at the start.
    - Must include the complete math equation up to and including the equals sign (e.g., "Q1. 5 x 1 =").
    - Must flatten text into a single horizontal line.
    - **NO** newline characters in the string.
2.  **Student Answer Field:**
    - Extract exactly the handwritten numbers placed next to the equals sign.
    - Return the answer as a string.
    - If the answer slot is blank, use an empty string `""`.

**WORKSHEET-SPECIFIC GUIDELINES:**
- **Ignore Teacher Markings:** The document contains red ink (large diagonal check marks and a score like "11/10" at the top right). Completely IGNORE these markings. Do not include them in your text or JSON.
- **Layout & Numbering:** The document has two columns. Track the question numbers carefully ("Q1", "Q2", etc.) to process the flow correctly. 
- **Handwriting Precision:** Look closely at pencil digit formation. Accurately distinguish **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**.

**PROCESSING ORDER:**
- Ensure every single question is captured.
- Sort the final output array by `question_number` in ascending order (e.g., 1 to 40).

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Multiplication equations.
*   **Examples Present:** None.
*   **Answer Format:** Handwritten numbers placed next to the equals sign in each equation.
*   **Structural Quirks:** The worksheet has two columns of questions. Questions are numbered sequentially, prefixed with "Q" (e.g., Q1., Q2.). There's a prominent red line (likely a teacher's check mark) diagonally across the first page, and another on the second page. A score "11/10" in red ink is present at the top right of the second page.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}