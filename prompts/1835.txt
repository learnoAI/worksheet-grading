You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. 
The worksheet contains a dense grid of vertical subtraction problems (3-digit minus 2-digit) with fill-in-the-blank answers. There are NO examples on the page; every item is a question.

**JSON SCHEMA ENFORCEMENT:**
Respond in the following JSON format, providing a list of all 40 questions and their answers:
{format_instructions}

**DO NOT** use dynamic keys. **DO NOT** nest the objects.
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES & FIELD DEFINITIONS:**
1. **"question_number" Field:**
   - Extract the question identifier exactly as written, including the 'Q' prefix and period (e.g., "Q1.", "Q2.", "Q40.").
2. **"question" Field:**
   - Extract **ONLY** the mathematical expression itself (e.g., "704 - 10").
   - **CRITICAL:** **DO NOT** include the question number prefix (e.g., "Q1.") in this field. 
   - The math problems are printed vertically on the page. You must flatten them into a single horizontal string using the format: `[Top Number] - [Bottom Number]`.
   - **NO** newline characters in the string.
3. **"student_answer" Field:**
   - Extract exactly what the student wrote in pencil below the horizontal line.
   - Return the answer as a string (e.g., "694", "285").
   - If the answer slot is completely blank, use an empty string `""`.

**ANTI-HALLUCINATION & GRID TRACKING (CRITICAL):**
- The questions are arranged in a very dense grid (4 columns per page).
- **Isolate Each Problem:** Mentally draw a tight bounding box around the single problem you are currently reading. **IGNORE** all numbers outside this box.
- **Prevent Visual Bleed-Over:** **NEVER** copy numbers from adjacent lines, rows, or columns. Treat each problem in complete isolation to prevent hallucinating repeated numbers (e.g., mistakenly repeating "740" or "718" where it does not belong).

**STEP-BY-STEP OCR VERIFICATION:**
To minimize errors, you MUST treat the printed text and handwriting as separate tasks:
1. **Step 1 (Question):** Focus ONLY on the printed numbers above the horizontal line. Verify the exact hundreds, tens, and units digits before writing the string.
2. **Step 2 (Answer):** Focus ONLY on the pencil markings below the line. Do not let the printed numbers influence your reading of the handwritten digits.

**DIGIT DISAMBIGUATION & HANDWRITING GUIDELINES:**
- Look very closely at digit formation to prevent minor digit misclassifications.
- Pay close attention to differentiating visually similar numbers, especially if the text is messy or compressed:
  - **2 vs 3:** Analyze the curves carefully (e.g., ensure you do not read a '2' as a '3').
  - **6 vs 7:** Check for loops vs sharp angles.
  - **1 vs 7:** Check for the horizontal top bar.
  - **3 vs 5** and **3 vs 8**
  - **8 vs 9**
  - **6 vs 0**
- If a handwritten number has strike-throughs or corrections, transcribe the final intended answer.

**PROCESSING ORDER:**
- Process column by column or row by row, but always sort the final JSON output by `question_number` in ascending numerical order (Q1. through Q40.).
- Double-check the exact 3-digit number in the question and the exact answer digits before finalizing each object.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an analysis of the key factors:

*   **Question Type:** Vertical subtraction (specifically 3-digit numbers minus 2-digit numbers).
*   **Examples Present:** None. There are no pre-solved examples provided on these pages; all items are numbered questions for the student to solve.
*   **Answer Format:** Fill in the blank. The student writes their answer directly below the horizontal line of each vertical math problem.
*   **Structural Quirks:** 
    *   **Layout-to-Text Conversion:** The math problems are presented vertically on the page but must be transcribed horizontally in the JSON (e.g., stacked "704" and "- 10" becomes "704 - 10").
    *   **Grid Layout:** The questions are arranged in a dense grid with 4 columns per page.
    *   **Numbering Style:** Question identifiers follow a specific format using a 'Q' prefix and a period (e.g., "Q1.", "Q2.").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}