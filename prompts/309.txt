You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and the corresponding circled student answer from the image provided. The worksheet contains number comparison questions where the student indicates their answer by circling the largest number in the sequence.

**JSON SCHEMA ENFORCEMENT:**
You must output a single JSON object with a root key `"questions_and_answers"` containing an array of objects.
{format_instructions}

Expected structure:
{
  "questions_and_answers": [
    {
      "question_number": 1,
      "question": "Q1. 1 , 3 , 7",
      "student_answer": "7"
    }
  ]
}

**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`.
**DO NOT** use dynamic keys or nest the objects differently.

**FORMATTING RULES:**

1.  **`question_number` (Integer):**
    - Must be extracted as a pure **integer** (e.g., `1`, `2`, `12`).
    - Strip any "Q" prefixes or periods (e.g., if the text says "Q1.", the value must be `1`).

2.  **`question` (String):**
    - Must include the full label (e.g., "Q1.", "Q2.") at the start.
    - **MAINTAIN STRICT VERBATIM SPACING:** Pay extremely close attention to spaces around punctuation. If the worksheet spaces numbers and commas like "1 , 3 , 7", you must output exactly `"Q1. 1 , 3 , 7"` (do not compress to "1, 3, 7").
    - Flatten the sequence into a single horizontal string.
    - **NO** newline characters in the string.

3.  **`student_answer` (String):**
    - Identify and extract the specific number that the student has **circled**.
    - Return the answer as a string (e.g., `"7"`).
    - If no number is circled, use an empty string `""`.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation (e.g., distinguishing **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**).
- If a number is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER:**
- The layout consists of two vertical columns. Ensure you process both columns accurately.
- Sort the final output array strictly by `question_number` in ascending order (1, 2, 3, ...).

=== WORKSHEET CONTEXT NOTES ===
Here is the analysis of the worksheet images based on the requested key factors:

*   **Question Type:** Number comparison (identifying and selecting the largest number in a given set).
*   **Examples Present:** None explicitly labeled. The worksheet begins directly with Q1.
*   **Answer Format:** Circling. The student answers by drawing a circle around the correct number directly within the provided sequence.
*   **Structural Quirks:** 
    *   The layout consists of two vertical columns.
    *   Questions follow a specific numbering format: "Q" followed by the number and a period (e.g., "Q1.", "Q12.").
    *   The target JSON expects the `question` field to capture the entire line of text (e.g., "Q1. 1 , 3 , 7"), including commas and spacing, while the `student_answer` field extracts only the value that has been circled.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}