You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet image. The worksheet contains number comparison problems where students indicate their answer by circling one of the numbers.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a valid JSON array containing objects for each question.
**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects.
Each object MUST contain exactly these three keys: `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES:**
1.  **`question_number` (Integer):**
    - Must be returned strictly as an **integer** (e.g., `1`, `2`, `15`).
    - **STRIP AWAY** any "Q" prefixes, periods, or punctuation marks. Do NOT return "Q1." or "1.".

2.  **`question` (String):**
    - Must contain the full question text exactly as it appears.
    - **MUST INCLUDE** the label prefix at the start (e.g., "Q1.", "Q21.").
    - Must include both numbers separated by a comma and spaces (e.g., "Q1. 1 , 13").
    - Flatten any vertical alignment into a single horizontal line. **NO** newline characters.

3.  **`student_answer` (String):**
    - Identify the answer by finding the **hand-drawn circle** around one of the numbers.
    - Extract exactly the number that is circled.
    - Return the answer as a string (e.g., "13", "14").
    - If no number is circled and the answer slot is blank, use an empty string `""`.

**HANDWRITING & LAYOUT EXTRACTION GUIDELINES:**
- The worksheet uses a two-column layout. Ensure you process both columns completely.
- Sort the final output array by `question_number` in ascending order.
- Look closely at digit formation. Distinguish carefully between **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**.
- If a handwritten number is messy or overlaps with the circle, select the most likely digit based on visual stroke evidence.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is the educational analysis:

*   **Question Type:** Number comparison (specifically, identifying and selecting the larger of two numbers).
*   **Examples Present:** None. There are no pre-solved examples provided on the worksheet.
*   **Answer Format:** Circling. The student answers by drawing a hand-drawn circle around the selected number.
*   **Structural Quirks:**
    *   The layout uses a two-column format.
    *   Questions are labeled with a specific prefix format: 'Q' followed by the number and a period (e.g., "Q1.", "Q21.").
    *   The question text itself consists of two numbers separated by a comma and spaces (e.g., "1 , 13").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}