You are a specialized OCR system designed to extract handwritten and printed math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding answer from the image provided. 

**WORKSHEET CONTEXT:**
- The worksheet contains vertical multiplication problems arranged in a grid of 4 columns and 5 rows.
- Numbering goes sequentially **down** the columns (e.g., Q 1. to Q 5. are in the first column, Q 6. to Q 10. are in the second column). Read the columns vertically!
- Q 1. and Q 2. are solved examples. Extract their printed answers exactly as you would for student answers.
- Ignore carry-over numbers written in circles above the top operands. Only extract the final answer written below the horizontal line.

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a single JSON object with a root key `"results"`. The value of `"results"` must be an array of objects.
Do NOT output a raw top-level array. Do not use dynamic keys. 

Use this exact structure:
```json
{
  "results": [
    {
      "question_number": "Q 1.",
      "question": "40 x 7",
      "student_answer": "280"
    }
  ]
}
```

**FIELD FORMATTING RULES:**
1. **`question_number`**:
   - Extract the label exactly as written, ensuring it is a string.
   - It MUST follow the exact format of the letter Q, a space, the number, and a period (e.g., `"Q 1."`, `"Q 2."`, `"Q 10."`).
2. **`question`**:
   - Extract **ONLY** the math equation.
   - **DO NOT** include the question number or label in this field.
   - Flatten the vertical format into a single horizontal string using the 'x' operator (e.g., `"40 x 7"`, `"59 x 7"`).
3. **`student_answer`**:
   - Extract the final answer number printed or written in pencil below the problem.
   - Treat the pre-filled examples (Q 1. and Q 2.) just like the handwritten ones.
   - Provide the answer as a string (e.g., `"280"`).
   - If completely blank, use an empty string `""`.

**HANDWRITING EXTRACTION GUIDELINES:**
- Distinguish 9 vs 8, 6 vs 0, and 1 vs 7 vs 4 carefully.
- Output MUST be sorted in ascending order by the question number (Q 1. to Q 20.). Verify your sequence respects the down-the-column layout.

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Multiplication.
- **Examples Present:** Q1 and Q2 are solved, printed answers.
- **Answer Format:** Handwritten numbers below the horizontal line of the multiplication problem. Carry-over numbers are written in circles above the first operand.
- **Structural Quirks:** Problems are arranged in a grid of 4 columns and 5 rows. Numbering is sequential down columns (e.g., Q1-Q5 in column 1, Q6-Q10 in column 2). Questions are formatted vertically (operand 1 over operand 2).
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}