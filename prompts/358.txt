You are a highly accurate OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet image. The worksheet consists of two vertical columns of "What comes after" number sequence problems. There are no pre-solved examples; all 40 items are student-answered fill-in-the-blanks.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a valid JSON object matching this exact structure. 
**CRITICAL:** Do NOT return a bare array. The root must be an object with the key `"questions"`.

```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 34 ,",
      "student_answer": "35"
    }
  ]
}
```

**FORMATTING & EXTRACTION RULES:**

1.  **Question Field (`question`):**
    - Must perfectly transcribe the printed text exactly as written.
    - Must include the question label (e.g., "Q1.", "Q2.").
    - **CRITICAL:** You MUST include the trailing comma and exact spacing exactly as it appears on the page (e.g., `"Q1. 34 ,"`). Do not omit the punctuation.
    - Flatten vertical text into a single horizontal string. **NO** newline characters.

2.  **Student Answer Field (`student_answer`) - STRICT NO-CALCULATION RULE:**
    - **CRITICAL: DO NOT AUTO-CORRECT MATH.** Transcribe the student's handwritten answers EXACTLY as they appear on the page. Do not attempt to solve the problems, calculate the next number in the sequence, or correct wrong answers.
    - Extract exactly the handwritten digits the student wrote in pencil on the horizontal blank line as a string. If the answer slot is entirely blank, use an empty string `""`.

3.  **Contextual Clues Strategy & Ambiguous Handwriting:**
    - This is a "+1 increment sequence" worksheet (e.g., 34 -> 35). **However**, the student made several math errors. You must extract exactly what is written, even if the math is wrong.
    - Use the mathematical context ONLY to help decipher highly ambiguous scribbles, but ALWAYS prioritize the visual strokes. Do not let the expected correct answer blind you to what is actually written.
    - **Known Visual Traps (Pay Extremely Close Attention):**
        - **Q14:** The sequence is "37 ,". The student incorrectly wrote **"36"**. Do NOT output "38". Look closely to ensure you do not misread the 6 as an 8.
        - **Q15:** The sequence is "50 ,". The student wrote **"51"**. Do NOT output "50". Look extremely closely at the stroke endings to catch the loosely written "1".
        - **Q24:** The sequence is "47 ,". The student incorrectly wrote **"46"**. Do NOT output "48".
        - **Q34:** The sequence is "49 ,". The student incorrectly wrote **"40"**. Do NOT output "50". 
    - **Digit Disambiguation:** Differentiate carefully between **0 vs 1**, **6 vs 8**, **4 vs 5**, and **0 vs 6 vs 9**.

4.  **Handling Overlays and Teacher Marks:**
    - **IGNORE TEACHER GRADING MARKS:** You will see red checkmarks, circles, and crossed-out text overlaying many answers. Ignore the red ink entirely and ONLY transcribe the student's underlying pencil answer.
    - Rely ONLY on visual evidence of the pencil strokes.

5.  **Layout Processing Order:**
    - The page is divided into two distinct vertical columns.
    - Track the Question IDs (Q1, Q2, etc.) to ensure correct sequencing.
    - Capture all 40 questions and sort the final JSON array strictly by `question_number` in ascending order (1 through 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here is the educational analysis:

*   **Question Type:** Number sequence (specifically finding the successor or "What comes after").
*   **Examples Present:** None. There are no pre-solved examples provided on the worksheet.
*   **Answer Format:** Fill in the blanks. The student writes a single handwritten number on a provided horizontal line next to each prompt.
*   **Structural Quirks:** 
    *   The page is divided into two distinct vertical columns.
    *   Questions use a specific prefix format: "Q" followed by the number and a period (e.g., "Q1.").
    *   The "question" string must capture the numbering, the prompt number, and the trailing comma (e.g., "Q1. 34 ,").
    *   Teacher grading marks (red checkmarks, circles, and crossed-out text) overlay many of the student's answers and need to be ignored when transcribing the written digits.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}