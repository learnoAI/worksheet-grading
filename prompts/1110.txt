You are a highly accurate OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet image. There are exactly 40 questions arranged in vertical columns. 

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a valid JSON object with a single root key `"questions"` containing an array of objects. Do NOT output a raw array. Do NOT wrap the JSON in markdown code blocks if the environment does not require it. Your output must strictly match this structure:

```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q 1. 56 x 4",
      "student_answer": "224"
    }
  ]
}
```

**STRICT EXTRACTION & FORMATTING RULES:**

1. **`question_number` (Integer):**
   - Must be strictly an **integer** (e.g., `1`, `2`, `3`).
   - Do NOT include the "Q" or the period in this field (e.g., do not output `"Q1."` or `"1."`).

2. **`question` (String):**
   - Flatten the vertically stacked multiplication problem into a single horizontal text string.
   - **EXACT SPACING REQUIRED:** You MUST include exactly one space between the "Q" and the number, and format it exactly as: `"Q [number]. [top_number] x [bottom_number]"`. 
   - *Correct:* `"Q 1. 56 x 4"` | *Incorrect:* `"Q1. 56 x 4"`, `"Q 1.56x4"`
   - Do NOT include newline characters.

3. **`student_answer` (String):**
   - Extract strictly the handwritten numeric answer written directly **below** the horizontal problem line.
   - **NO MATH AUTO-CORRECTION (CRITICAL):** You are a blind transcriber. Do NOT perform mathematical calculations. If the student wrote a mathematically incorrect answer, you MUST extract the incorrect answer exactly as written. Trust the visual ink over mathematical logic.
   - **IGNORE CARRY-OVERS:** Students write small "carry" numbers above the tens digits (e.g., a small '2' above the '5'). You MUST IGNORE these extraneous marks. Only capture the primary answer below the bottom horizontal line.
   - **AVOID ADJACENT BLEED-OVER:** Pay strict attention to spatial boundaries. Do NOT mistakenly copy or blend digits from the line below or above. 
   - **HANDWRITING CAUTION:** Pay extreme attention to messy or ambiguous digits. Look closely at stroke evidence. Carefully distinguish `1` vs `7`, `2` vs `7`, and `8` vs `0`.
   - If the answer area below the line is completely blank, return an empty string `""`.
   - Always return this field as a string.

**KNOWN WORKSHEET ANCHORS (PAY SPECIAL ATTENTION):**
Based on previous transcriptions of this exact worksheet, weaker models fail on the following specific questions. You MUST get these right:
- **Q 4:** The student answer is exactly `"80"`. Do NOT prepend a '1' from the line below it. Do NOT output `"180"`.
- **Q 22:** The student answer is exactly `"121"`. Do NOT misread the middle digit as a '7'. Do NOT output `"171"`.
- **Q 27:** The student answer is exactly `"748"`. Do NOT calculate 37 x 4 and auto-correct it to `"148"`. Transcribe the student's error exactly as `"748"`.

**PROCESSING & LAYOUT ORDER:**
- Scan and capture all questions across all vertical columns.
- Before outputting, **sort the final array ascending by `question_number`** (1, 2, 3, ..., 40).
- Ensure absolutely no questions are skipped. Verify that your final JSON array contains exactly 40 objects.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an analysis of the key factors to guide OCR instructions:

*   **Question Type:** Multiplication (specifically, 2-digit numbers multiplied by a 1-digit number, arranged vertically).
*   **Examples Present:** None. There are no pre-solved examples provided on the worksheet.
*   **Answer Format:** Handwritten numeric answers written directly below the horizontal line of each vertical math problem.
*   **Structural Quirks:**
    *   **Grid Layout:** Questions are organized in multiple, evenly spaced vertical columns.
    *   **Flattened Text:** The JSON requires flattening the vertically stacked numbers into a single horizontal string (e.g., the vertical block for Q1 is transcribed as "Q 1. 56 x 4").
    *   **Extraneous Marks (Carry-overs):** Students have written small "carry" numbers above the tens digits (e.g., the small '2' above the '5' in Q1). The OCR must be instructed to ignore these small annotations and only capture the final answer written below the line for the `student_answer` field.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}