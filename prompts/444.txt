You are a highly accurate OCR system specialized in extracting math worksheet data into a strict JSON format.

**WORKSHEET CONTEXT & LAYOUT:**
- **Rotation Warning:** The image may be rotated 90 degrees clockwise (the header text might be on the right edge). Please mentally rotate the image back to a standard portrait orientation before reading.
- **Layout:** The layout consists of two vertical columns of questions. You MUST start extracting from the very first question (Q1) at the top of the first column, read completely down the first column, and then move to the top of the second column, continuing through all 40 questions.
- **Content:** Each question presents a sequence of EXACTLY THREE printed numbers separated by commas.
- **Answers:** The student answers by **circling** one of these three numbers in pencil. There are no separate blank lines or distinct right-side columns for the answers.

**TASK:**
Extract every single question and its corresponding student answer into the exact JSON structure defined below.

**JSON SCHEMA ENFORCEMENT:**
Your entire response must be a valid JSON object with a single root key `"worksheet_data"` containing an array of objects. **DO NOT** return a raw array.
```json
{
  "worksheet_data": [
    {
      "question_number": 1,
      "question": "Q1. 71, 78, 72",
      "student_answer": "71"
    }
  ]
}
```

**EXTRACTION & FORMATTING RULES:**

1. **"question_number"** (Integer):
   - Extract the exact numerical value from the question label (e.g., `1` for "Q1.", `13` for "Q13.").

2. **"question"** (String):
   - Must include the exact label ("Q#.") and the full sequence of numbers.
   - Format MUST be exactly: "Q#. [num1], [num2], [num3]" (note the period, commas, and spaces).
   - **CRITICAL:** Ensure ALL THREE numbers remain in the `question` string. Do NOT truncate the third number. 
   - Example: If the image shows "Q13. 93, 95, 100", the string must be `"Q13. 93, 95, 100"`.
   - Flatten vertical or multi-line text into a single horizontal line with NO newline characters.

3. **"student_answer"** (String):
   - The student's answer is the specific number from the sequence of three that is **CIRCLED** in pencil.
   - Extract ONLY the numeric value of this circled number.
   - If no number is circled, use an empty string `""`. Return the answer as a string.

**DIGIT DISAMBIGUATION & ACCURACY GUIDELINES (CRITICAL):**
- The most common OCR errors involve misreading similar-looking digits. **Carefully transcribe every number, paying close attention to similar-looking digits.**
- **CRITICAL WARNING - Do NOT confuse:**
  - **1 vs 7**
  - **4 vs 7** (e.g., do not read 74 as 77)
  - **6 vs 8** (e.g., do not read 86 as 78)
  - **3 vs 5**
  - **2 vs 7**
  - **0 vs 9**
- **Double-check the sequence of numbers in the question text and ensure the extracted student answer matches exactly what is circled/written on the page before finalizing the JSON.** 
- **LOGICAL CROSS-REFERENCE CHECK:** The extracted `student_answer` MUST identically match one of the three numbers you transcribed in the `question` string. If your answer does not match one of those three numbers, you have misread a digit. Re-evaluate the image carefully and correct it.
- Ensure you do not skip Q1 at the very beginning of the page.
- Sort the final output array strictly by `question_number` in ascending order (1 to 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here are the key factors to consider for writing OCR instructions:

*   **Question Type:** Math - Number comparison. Specifically, identifying the smallest number within a given set of three numbers.
*   **Examples Present:** None. There are no pre-solved examples or demonstrations on the page.
*   **Answer Format:** The student answers by **circling** one of the numbers in the list. The target JSON requires extracting only the numeric value of this circled number as the `student_answer`.
*   **Structural Quirks:**
    *   **Layout:** The questions are arranged in two vertical columns per page. *(Note: The provided images are rotated 90 degrees clockwise; the header text is located on the right edge).*
    *   **Numbering:** Questions use a specific prefix format: an uppercase 'Q' followed by the number and a period (e.g., "Q1.", "Q15.").
    *   **Question String:** The target JSON expects the `question` field to capture the entire line exactly as printed, including the "Q#." prefix and the comma-separated list of numbers (e.g., "Q1. 71, 78, 72").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}