You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. Read down the vertical columns to capture all questions sequentially (Q1 to Q20).

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a valid JSON object matching this exact structure, enclosed in a root `"questions_and_answers"` wrapper:
{
  "questions_and_answers": [
    {
      "question_number": 1,
      "question": "Q 1. Count the tree",
      "student_answer": "1"
    },
    {
      "question_number": 2,
      "question": "Q 2. Count the birds",
      "student_answer": "2"
    }
  ]
}

**STRICT EXTRACTION RULES:**

1. **question_number (Integer):**
   - Extract ONLY the numerical integer value (e.g., `1`, `2`, `10`). 
   - DO NOT output strings here. DO NOT include "Q" or periods (e.g., `1` is correct, `"Q1."` is incorrect).

2. **question (String - FULL EXACT TRANSCRIPTION):**
   - **CRITICAL:** Capture the COMPLETE question string. You MUST include both the question label/number AND the full instruction text (e.g., "Q 1. Count the tree"). Do not stop extracting after the period!
   - Transcribe the printed text EXACTLY as it appears. DO NOT look at the pictures and guess words. DO NOT substitute synonyms or use visual interpretations. 
   - **Self-Correction Step:** Double-check the exact wording of the noun phrases at the end of each question before outputting.
   - If the text says "tree", write "tree" (NOT "mushrooms").
   - If the text says "caps", write "caps" (NOT "hats").
   - If the text says "suns", write "suns" (NOT "stars").
   - If the text says "soccer balls", write "soccer balls" (NOT "footballs").
   - Must flatten any vertical text into a single horizontal line with NO newline characters. Preserve original spacing and punctuation.

3. **student_answer (String):**
   - Extract exactly what the student wrote in pencil in the smaller, separate rectangular box next to the images.
   - Return the numerical answer as a string (e.g., `"2"`).
   - If the answer slot is blank, use an empty string `""`.
   - Distinguish ambiguous handwriting carefully (e.g., **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**).

**PROCESSING ORDER:**
- The worksheet is divided into two vertical columns. Track the Question IDs to read them correctly.
- Ensure all 20 questions are extracted.
- Sort the final output array strictly by `question_number` in ascending order (1 to 20).

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Counting objects and writing the corresponding number.
*   **Examples Present:** None.
*   **Answer Format:** Writing numbers in a designated rectangular box next to the objects.
*   **Structural Quirks:** The worksheet is divided into two vertical columns. Each question consists of an image box containing objects and a separate, smaller box for the numerical answer. Questions are numbered sequentially from Q1 to Q20.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}