You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet images.

**WORKSHEET CONTEXT & EXTRACTION RULES:**
1. **Layout & Order:** Questions are arranged vertically in two distinct columns. Process column by column, but sort the final output by `question_number` in ascending order.
2. **Exclusions:** 
   - IGNORE the bilingual header instructions at the top ("Divide the following." / "भाग करो.").
   - IGNORE any partially visible, cut-off text on the far right margin.
   - IGNORE all red teacher grading checkmarks or strokes. Extract ONLY the original blue/pencil handwritten text.
3. **Answer Pattern:** The student answers follow a strict pattern: "[Quotient] R [Remainder]" (e.g., "23 R 1", "5 R 0"). Look for this pattern after the equals sign.

**FIELD SPECIFIC RULES:**
- **`question_number`** (integer): The numeric identifier of the question.
- **`question`** (string): 
  - **CRITICAL:** Preserve exact spacing for the label. You MUST use "Q 1.", "Q 2.", etc. (with a space between the 'Q' and the number). Do NOT use "Q1.".
  - Include the division equation up to and including the equals sign (e.g., "Q 1. 70 ÷ 3 =").
  - Flatten into a single horizontal line. NO newline characters.
- **`student_answer`** (string): 
  - Extract the alphanumeric text written by the student.
  - Pay close attention to digit formation (9 vs 8, 6 vs 0, 1 vs 7 vs 4).
  - If blank, return an empty string `""`.

**JSON SCHEMA ENFORCEMENT:**
You MUST wrap the array of questions within a root `"questions"` object. Do NOT return a raw JSON array. 
Respond EXACTLY matching this structure:

{
  "questions": [
    {
      "question_number": 1,
      "question": "Q 1. 70 ÷ 3 =",
      "student_answer": "23 R 1"
    },
    {
      "question_number": 2,
      "question": "Q 2. 25 ÷ 5 =",
      "student_answer": "5 R 0"
    }
  ]
}

Return ONLY valid JSON. No conversational text or markdown code blocks outside the JSON itself.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and the target JSON output, here is an analysis of the key factors to help refine OCR instructions:

*   **Question Type:** Math - Division (specifically calculating quotients and remainders).
*   **Examples Present:** None. There are no pre-solved examples; all questions have been answered by the student.
*   **Answer Format:** Handwritten text placed immediately after the equals sign. The answers consistently follow a specific alphanumeric pattern: `[Quotient] R [Remainder]` (e.g., "23 R 1", "5 R 0").
*   **Structural Quirks:**
    *   **Two-Column Layout:** The questions are arranged vertically in two distinct columns per page.
    *   **Numbering Format:** Questions use a specific prefix structure: "Q " followed by the number and a period (e.g., "Q 1.").
    *   **Overlaid Marks:** There are red teacher grading checkmarks drawn over or through the handwritten student answers. The OCR must be instructed to ignore these red marks and only extract the blue handwritten text.
    *   **Extraneous Text:** The second image has a partially visible, cut-off column of text on the far right margin that belongs to a different page and should be ignored.
    *   **Bilingual Header:** The instructions at the top are provided in both English ("Divide the following.") and Hindi ("भाग करो.").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}