You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question (Q1 through Q40) and its corresponding student answer from the provided images. Consolidate the data from all columns and both images into a single continuous sequence.

**JSON SCHEMA ENFORCEMENT:**
Respond ONLY with a JSON array of objects. 
DO NOT use dynamic keys. DO NOT nest the objects.
KEYS MUST BE exactly: `"question_number"`, `"question"`, `"student_answer"`.

**FIELD-LEVEL EXTRACTION RULES:**

1. **"question_number"**:
   - Must include the "Q", the number, AND the trailing period (e.g., `"Q1."`, `"Q2."`). Do not drop the period.

2. **"question"**:
   - Extract ONLY the mathematical expression (e.g., `"850 - 700"`).
   - **DO NOT** include the question number or prefix in this field.
   - Flatten the vertical math into a single horizontal line formatted as "TopNumber - BottomNumber" with spaces around the minus sign.
   - NO newline characters in the string.

3. **"student_answer"**:
   - Extract EXACTLY what the student wrote in pencil below the line. Return as a string.
   - **TRANSCRIBE LITERALLY:** Do not calculate or "correct" the math! If the student wrote an mathematically incorrect answer (e.g., writing "757" for a problem that equals 457), you must extract exactly what they wrote ("757").
   - **LEADING ZEROS:** Include leading zeros *only* if the student explicitly wrote them (e.g., if they wrote "024", output `"024"`; if they wrote "70", output `"70"`). Do not hallucinate or auto-fill leading zeros.
   - If the answer slot is totally blank, use an empty string `""`.

**HANDWRITING & OCR GUIDELINES:**
- Look extremely closely at digit formation to disambiguate visually similar numbers:
  - Distinguish **4** vs **7**, **1** vs **9**, and **8** vs **9** (e.g., carefully check if a number is "914" vs "800", or "114" vs "119").
- Read the entire document to ensure all 40 questions are captured.
- Sort the final JSON array by `question_number` in ascending numerical order (Q1. to Q40.).

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Subtraction problems.
*   **Examples Present:** None clearly marked as examples. The format is consistent throughout.
*   **Answer Format:** Hand-written numbers below a horizontal line under each subtraction problem.
*   **Structural Quirks:** The problems are arranged in multiple vertical columns. Each problem has a clear "Q[number]." identifier. The arithmetic is presented vertically. The worksheet is split across two images, requiring consolidation of the continuous sequence of questions (Q1 to Q40).
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}