You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. 

**JSON SCHEMA ENFORCEMENT:**
You MUST wrap your output in a single root object with the key `"questions"`. Do not return a raw array.
Respond EXACTLY in the following JSON format:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 11 + 2 =",
      "student_answer": "13"
    },
    {
      "question_number": 8,
      "question": "Q8. 18 + 2 =",
      "student_answer": "२०"
    }
  ]
}
```

**FORMATTING AND EXTRACTION RULES:**

1. **`question_number` (Integer):**
   - Extract ONLY the numeric value for the question number.
   - Strip any alphabetical prefixes (e.g., change "Q1" to the integer `1`).
   - Must be formatted as an integer, NOT a string.

2. **`question` (String):**
   - Must include the full label prefix (e.g., "Q1.", "Q2.") and the equation exactly as printed, ending with the equals sign (e.g., "Q1. 11 + 2 =").
   - Flatten vertical text into a single horizontal line.
   - **NO** newline characters in the string.

3. **`student_answer` (String):**
   - Extract exactly what the student wrote in pencil after the equals sign.
   - **CRITICAL - PRESERVE SCRIPT:** Preserve the exact original script and characters of the student's handwriting. If the student wrote in Devanagari numerals (e.g., २०, २१, २२), you MUST output those exact Devanagari characters (or their Unicode equivalents, e.g., `\u0968\u0966`). **DO NOT** translate or transliterate non-Latin/Devanagari numerals into standard Arabic numerals (e.g., do not turn २० into 20).
   - **IGNORE GRADING MARKS:** Ignore any grading marks (like red lines crossing through numbers) and decipher the underlying handwritten digits.
   - **BLANKS:** If the answer slot is completely blank, return an empty string `""`.
   - Always return the answer as a string, even if it represents a number.

**PROCESSING ORDER:**
- The worksheet contains multiple vertical columns. Handle the columns correctly by reading down the first column, then down the next, tracking the Question IDs.
- Final output MUST be sorted by `question_number` in ascending order (1, 2, 3...).

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Addition problems (e.g., "11 + 2 =").
- **Examples Present:** None clearly marked as examples. All lines appear to be questions for the student to solve.
- **Answer Format:** Fill in the blank (or rather, write the answer after the equals sign). The student writes the sum directly after the equation. Some answers are in English numerals (e.g., 13), while others are in Devanagari numerals (e.g., २०, २१).
- **Structural Quirks:** The problems are laid out in two vertical columns across two pages/images. The numbering is prefixed with "Q" (e.g., "Q1.", "Q2."). Some answers have marks from grading (like a red line crossing through some numbers), but the underlying written answer is still decipherable. The OCR needs to capture both the equation part up to the equals sign as the 'question' and the handwritten number following it as the 'student_answer', recognizing Devanagari numerals where present.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}