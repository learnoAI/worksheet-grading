You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the provided worksheet images.

**JSON SCHEMA ENFORCEMENT:**
You must return a JSON object with a single root key `"questions"` containing an array of objects. Use the exact structure below:
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 17 x 1 =",
      "student_answer": "17"
    }
  ]
}
```

**EXTRACTION & FORMATTING RULES:**

1. **`question_number` (Integer):**
   - Extract ONLY the numeric value of the question number. 
   - Ensure this is formatted as an **integer** (e.g., `1`, `2`), **NOT** a string (e.g., "Q1" or "1"). Drop the "Q" prefix and the period.

2. **`question` (String):**
   - Include the full printed text of the question exactly as it appears, including the label, the equation, and the equals sign.
   - Example: `"Q1. 17 x 1 ="`
   - Flatten any vertical text into a single horizontal line with single spaces between elements.
   - Do NOT include newline characters.

3. **`student_answer` (String):**
   - Extract exactly what the student wrote in pencil to the right of the equals sign (`=`).
   - Look closely at digit formation (e.g., distinguish 9 vs 8, 6 vs 0, 1 vs 7 vs 4).
   - If the answer slot is blank, return an empty string `""`.
   - The output must be a string, even though the content represents a number (e.g., `"17"`).

**LAYOUT & NOISE HANDLING (CRITICAL):**
- **Ignore Grading Marks:** There are large, diagonal handwritten lines (strikes) across several questions and answers used for grading. Ignore these entirely. Do not interpret them as numbers (like '1') or symbols (like '/').
- **Ignore Extraneous Text:** Do NOT extract page headers (Name, Token Number, Marks) or footer watermarks (e.g., "POCO M5", timestamps).
- **Column Layout:** The questions are arranged in two vertical columns. Make sure to track the question numbers (Q1 to Q40) carefully to ensure no items are missed.
- **Sorting:** Sort the final output array strictly in ascending order by `question_number` (1 through 40).

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an analysis of the key factors to guide OCR instruction writing:

*   **Question Type:** Basic arithmetic (multiplication facts for the number 17).
*   **Examples Present:** None. All numbered items (Q1-Q40) are exercises intended for the student to solve.
*   **Answer Format:** Fill-in-the-blank style. The student writes the answer directly to the right of the equals sign (`=`) on the same line.
*   **Structural Quirks:** 
    *   **Two-Column Layout:** The questions are arranged in two distinct vertical columns on each page.
    *   **Numbering Style:** Questions use a specific prefix format: "Q" followed by the number and a period (e.g., "Q1.", "Q2.").
    *   **Grading Marks:** There are large, diagonal handwritten lines striking through several questions/answers on both pages. OCR instructions must specify ignoring these grading marks so they are not interpreted as numbers (like a '1') or symbols (like a '/').
    *   **Extraneous Text:** The pages contain headers (Name, Token Number, Marks) and footer watermarks ("POCO M5", timestamps) that should be excluded from the final JSON output.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}