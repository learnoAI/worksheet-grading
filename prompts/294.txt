You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. 

**WORKSHEET CONTEXT:**
- **Question Type:** Number comparison. Students are given a pair of numbers and must identify the larger number.
- **Answer Format:** The student circles the larger number. You must identify the circled number and extract it as the `student_answer`.
- **Layout:** The questions are arranged in two vertical columns.

**JSON SCHEMA ENFORCEMENT:**
You must output a single JSON object containing a `"questions"` key. The value of this key must be an array of objects. 
DO NOT output a raw array `[...]`. You must output `{"questions": [...]}`.

Use exactly this structure:
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 17 , 15",
      "student_answer": "17"
    }
  ]
}

**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the objects further than the `"questions"` array.

**FORMATTING RULES:**
1.  **`question_number` Field:**
    - MUST be an **integer** (e.g., `1`, `2`, `3`).
    - Extract ONLY the numerical value. **DO NOT** include the "Q" prefix or the period (e.g., use `1` instead of "Q1.").
2.  **`question` Field:**
    - Must include the full original printed text for that specific item.
    - Include the label, period, and the number pair separated by a comma (e.g., `"Q1. 17 , 15"`).
    - Flatten vertical text into a single horizontal line.
    - **NO** newline characters in the string.
3.  **`student_answer` Field:**
    - Look for the number that the student circled in pencil/pen.
    - Extract exactly the circled digit(s).
    - If no number is circled, use an empty string `""`.
    - Return the answer as a string (e.g., `"17"`).

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation and circling marks.
- Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
- If a number or circle is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER:**
- Handle multiple columns correctly by tracking the numerical IDs.
- Sort the final output strictly by `question_number` ascending (1 to 40).

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Number comparison (identifying the larger number).
- **Examples Present:** No solved examples are provided.
- **Answer Format:** Students answer by circling the larger number in each pair. The circled number should be extracted as the `student_answer`.
- **Structural Quirks:** The layout features two vertical columns of questions. Each question is numbered sequentially (e.g., 'Q1.', 'Q2.') followed by a pair of numbers separated by a comma.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}