You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every subtraction question and its corresponding student answer from the provided worksheet images.

**WORKSHEET CONTEXT:**
- The problems are vertical subtraction equations, laid out in a grid with multiple vertical columns. 
- You must reconstruct the vertical equations into horizontal text.
- Answers are handwritten below a horizontal line. Ignore any red checkmarks or grading marks over the answers.
- Ignore general instructions like "Subtract the following" or "घटाओ".

**JSON SCHEMA ENFORCEMENT:**
You MUST return a single JSON object with a `"questions"` key containing an array of objects. 
**DO NOT** return a top-level array. **DO NOT** use dynamic keys. 
Use EXACTLY this structure:

```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 280 - 10",
      "student_answer": "270"
    },
    {
      "question_number": 2,
      "question": "Q2. 280 - 20",
      "student_answer": "260"
    }
  ]
}
```

**FORMATTING RULES FOR EACH KEY:**

1.  **`question_number` (Integer):** 
    - Must be an **integer** type (e.g., `1`, `2`, `15`), NOT a string.
    - Extract the numeric part only. Strip the "Q" prefix and any punctuation (If the text says "Q1.", output `1`).

2.  **`question` (String):**
    - Flatten the vertical math problem into a single horizontal line.
    - Must include the full label at the start followed by the equation (e.g., `"Q1. 280 - 10"`).
    - **NO** newline characters (`\n`) in the string. Provide exactly one space around the minus sign.

3.  **`student_answer` (String):**
    - Extract exactly what the student wrote in pencil below the line.
    - Return the answer as a **string** (e.g., `"270"`).
    - If the answer slot is completely blank, use an empty string `""`.

**HANDWRITING & PROCESSING GUIDELINES:**
- Look closely at digit formation (distinguish 9 vs 8, 6 vs 0, 1 vs 7 vs 4). If messy, select the most likely digit based on visual strokes.
- Track the question IDs carefully across the vertical columns to ensure no questions are missed.
- **CRITICAL:** Sort the final output strictly by `question_number` in ascending order (from 1 up to the highest number found, e.g., 40).

=== WORKSHEET CONTEXT NOTES ===
Here's a breakdown of the worksheet's key features to guide OCR extraction:

*   **Question Type:** Subtraction problems presented in a vertical format.
*   **Examples Present:** None clearly marked as examples. However, all questions have handwritten answers.
*   **Answer Format:** Handwritten numbers placed underneath a horizontal line, below the two numbers to be subtracted. Some answers have red checkmarks over them.
*   **Structural Quirks:**
    *   The page is divided into multiple vertical columns.
    *   Each question is numbered sequentially across rows (e.g., Q1, Q6, Q11, Q16 in the first row across different columns; Q21, Q26, Q31, Q36 in the corresponding row of the second image).
    *   The layout is a grid-like structure.
    *   The "question" part in the target JSON needs to reconstruct the vertical problem into a horizontal string (e.g., "Q1. 280 - 10").
    *   There is Hindi text "घटाओ" (Subtract) below the English instruction "Subtract the following".
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}