You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. There are exactly 40 questions arranged in a dense 4-column grid. Ignore all extraneous grading marks (such as large diagonal lines, grading notes like "AI" or "28/8") and bottom watermarks/timestamps.

**CRITICAL RULES: NO AUTOCORRECT AND NO EXTRAPOLATION**
1. **DO NOT CORRECT THE STUDENT'S MATH:** Your absolute highest priority is verbatim visual transcription. You must capture exactly what the student wrote, even if it is mathematically incorrect. (e.g., If the printed question is "22 - 12" and the handwritten answer is "16", you MUST output `"16"`, NOT "10"). 
2. **DO NOT GUESS OR CONTINUE PATTERNS:** Read every single digit directly from the image. Do not assume a sequence continues. The base numbers change abruptly (e.g., moving from questions with "27 - ..." to "28 - ..."). You must visually verify every number rather than autocompleting a pattern.

**JSON SCHEMA ENFORCEMENT:**
Your output must be a single JSON object containing a `"results"` key. The value of `"results"` must be an array of exactly 40 objects. 
Use exactly this structure:
```json
{
  "results": [
    {
      "question_number": 1,
      "question": "Q1. 21 - 10",
      "student_answer": "11"
    }
  ]
}
```

**FIELD DEFINITIONS & FORMATTING RULES:**

1. **`"question_number"` (Integer):**
   - Must be a pure integer (e.g., `1`, `2`, `3`). 
   - **DO NOT** include the "Q" prefix or the period here.

2. **`"question"` (String):**
   - Must start with the normalized question label exactly formatted as `"Q" + number + ". "` (e.g., `"Q1. "`, `"Q2. "`), standardizing any irregular spacing from the page.
   - Must flatten the vertically stacked math problem into a single horizontal string.
   - Use a single space around the minus sign (e.g., `"21 - 10"`).
   - Complete Example: `"Q1. 21 - 10"`.
   - **NO** newline characters in the string.

3. **`"student_answer"` (String):**
   - Extract exactly what the student wrote in pencil below the horizontal line.
   - If the answer slot is blank, use an empty string `""`.
   - Must be returned as a string (e.g., `"11"`, `"16"`).
   - Carefully distinguish handwritten digits: **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.

**PROCESSING ORDER & VERIFICATION:**
- Read through the dense 4-column layout methodically. 
- Ensure the handwritten answer you are capturing belongs to the exact printed question number above it. Pay extreme attention to Q28 through Q40, ensuring no shifting of answers occurs.
- Sort the final `"results"` array strictly by `question_number` in ascending order (from `1` to `40`). Verify that no question number is skipped, duplicated, or hallucinated.

=== WORKSHEET CONTEXT NOTES ===
Here is the educational analysis based on the provided worksheet images and target JSON:

*   **Question Type:** Subtraction (two-digit numbers).
*   **Examples Present:** None. All questions appear to be solved by the student with no predefined example block.
*   **Answer Format:** Handwritten numeric answers placed directly below the horizontal line of each vertical math problem. 
*   **Structural Quirks:**
    *   **Grid Layout:** Questions are arranged in a dense 4-column layout. Reading order needs to proceed column-by-column or row-by-row consistently (the JSON processes them sequentially by question number).
    *   **Format Transformation:** The printed worksheet displays the math problems vertically (stacked numbers). However, the target JSON requires these to be transcribed as single horizontal string equations (e.g., a stacked `21` over `- 10` must become `"Q1. 21 - 10"`).
    *   **Numbering Style:** Questions are prefixed with "Q" followed by the number and a period, often with varying spacing (e.g., "Q 1." vs "Q1.").
    *   **Interfering Marks:** There is a large, diagonal handwritten grading line across the first page, along with grading notes at the top ("AI", "28/8") and watermarks/timestamps at the bottom. These must be ignored during extraction.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}