You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every division question and its corresponding handwritten student answer from the provided worksheet image. The worksheet contains basic arithmetic division problems in a fill-in-the-blanks format. Ignore page headers like "Divide" or "भाग करो".

**JSON SCHEMA ENFORCEMENT:**
You MUST return a JSON object with a single root key `"worksheet_data"`. This key must contain an array of objects.
Do NOT use dynamic keys (like "Q1", "A1").
Your output must strictly follow this exact structure and data type format:
{
  "worksheet_data": [
    {
      "question_number": 1,
      "question": "Q1. 15 \u00f7 5 =",
      "student_answer": "3"
    }
  ]
}

**EXTRACTION & FORMATTING RULES:**
1. **`question_number` (Integer):**
   - You MUST extract ONLY the numerical integer value from the question label.
   - Strip the "Q" and the period "." (e.g., for "Q1.", output the integer `1`, not the string `"Q1."`). 
2. **`question` (String):**
   - Must include the full original label (e.g., "Q1."), the numbers, the division symbol (use standard unicode `\u00f7` or `÷`), and the equals sign (`=`).
   - Flatten vertical/stacked text into a single horizontal string with spaces (e.g., "Q1. 15 ÷ 5 =").
   - **NO** newline characters in the string.
3. **`student_answer` (String):**
   - **CRITICAL - STRICT LITERAL TRANSCRIPTION:** Transcribe the student's handwritten pencil answer EXACTLY as it appears.
   - **DO NOT AUTO-CORRECT INCORRECT MATH.** For example, if the math is "42 ÷ 6 =" but the student wrote "39", you MUST output "39".
   - Extract the writing immediately following the equals sign.
   - Return the answer as a string. If the answer slot is totally blank, use an empty string `""`.

**LAYOUT & ALIGNMENT GUIDELINES:**
- The worksheet is structured in a **two-column format** separated by a thin vertical line.
- Read carefully down the columns and accurately track the question numbers sequentially (from 1 up to 40).
- **Enforce row-by-row alignment:** Ensure that you do not skip any problems or misalign the math problem with the wrong question number. Pay exceptional attention to the numbering and alignment in the second half of the page.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation. Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
- If a number is messy, select the most likely digit based strictly on visual stroke evidence, not on what the correct math answer should be.

Return ONLY the valid JSON object. Do not include any introductory text, commentary, or markdown formatting blocks (do not use ```json ... ```). Output the raw JSON directly.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON, here is an analysis of the key factors to help write better OCR instructions:

*   **Question Type:** Basic arithmetic division problems.
*   **Examples Present:** None. There are no pre-solved examples; the worksheet begins directly with the first question.
*   **Answer Format:** Fill-in-the-blanks. The student handwrites the numerical answers in the empty space directly following the equals ("=") sign.
*   **Structural Quirks:**
    *   The problems are laid out in a distinct two-column format on each page, separated by a thin vertical line.
    *   Questions use a specific numbering prefix format: "Q" followed by the number and a period (e.g., "Q1.", "Q2.").
    *   The header includes bilingual text ("Divide" and "भाग करो" which is Hindi for 'divide').
    *   The target JSON captures the full question string including the numbering, operands, division symbol ($\div$), and equals sign before recording the handwritten student answer separately.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}