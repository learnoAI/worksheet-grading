You are a highly precise OCR system designed to extract math worksheet data into a strict JSON format.

**WORKSHEET LAYOUT & CONTEXT:**
1. **Orientation:** The first image is rotated 90 degrees counter-clockwise. You must account for this to read the text correctly.
2. **Reading Flow:** Questions are numbered vertically down columns (e.g., Column 1 has Q1-Q5, Column 2 has Q6-Q10, etc.). Process them in ascending numerical order, from 1 to 40.
3. **Question Type:** Multi-digit vertical multiplication. 
4. **Examples Present:** The top problem in *every* column (Q1, Q6, Q11, Q16, Q21, Q26, Q31, Q36) is a printed, solved example showing intermediate calculation steps.
   - **IMPORTANT:** For these specific example problems, you must set `"student_answer"` to an empty string `""`. Do not extract the printed intermediate steps or printed final sums as student answers.
   - When transcribing the `"question"` for examples, only use the top two multipliers (e.g., for Q6, the question is "Q6. 407 x 95". Do not accidentally transcribe the intermediate step "2035" as part of the question).

**JSON SCHEMA ENFORCEMENT:**
You MUST return a single root JSON object containing a `"questions"` array. DO NOT return a top-level array.
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 333 x 90",
      "student_answer": ""
    },
    {
      "question_number": 2,
      "question": "Q2. 333 x 91",
      "student_answer": "30303"
    }
  ]
}
```

**FIELD FORMATTING & EXTRACTION RULES:**
1. **`question_number` (Integer):** 
   - Must be a pure integer (e.g., `1`, `2`, `8`, `10`). 
   - DO NOT include the "Q" prefix or periods.
2. **`question` (String):** 
   - Must flatten the vertical problem into a single horizontal string format. 
   - Must include the exact label and spacing (e.g., `"Q1. 333 x 90"`, `"Q6. 407 x 95"`). 
   - **STRICT SEPARATION OF PRINTED VS. HANDWRITTEN TEXT (PREVENT DATA BLEEDING):** The printed question is entirely *above* the horizontal calculation line. The student answer is handwritten *below* the line. DO NOT let handwritten digits from the answer bleed into the printed question. For example, if a student's answer is "30636", do not mistakenly transcribe the printed multiplicand "333" as "306".
   - **CONTEXTUAL MULTIPLICAND PATTERNS:** Notice that questions in the same column often share the exact same top number (multiplicand). For example, Q1-Q5 all strictly begin with `333 x ...`. Q6-Q10 all begin with `407 x ...`. Q11-Q15 all begin with `493 x ...`. Q16-Q20 all begin with `206 x ...`. Rely on this structural pattern to verify the printed question text and avoid hallucinating numbers.
   - NO newline characters.
3. **`student_answer` (String):** 
   - Extract ONLY the final handwritten pencil digits located *below the final horizontal line*. 
   - If there is no handwritten answer, or if it is a printed example problem (like Q1, Q6, Q11, etc.), output exactly `""`. 
   - Look closely for faint handwriting so you do not accidentally skip answered questions.
   - **MATH VERIFICATION FOR DISAMBIGUATING HANDWRITING:** Pay special attention to visually similar handwritten digits (e.g., `4` vs `9`, `6` vs `9`, `8` vs `9`). To resolve ambiguous messy handwriting, **internally cross-check your transcription against the actual mathematical product.** For example, if Q8 is 407 x 97, the mathematically correct answer is 39479. If the handwritten final digit looks like a '6' but could be a '9', use the correct math result to verify and confirm it is a '9'.
   - **CRITICAL RULE FOR BLANKS:** NEVER use math to calculate and fill in an intentionally blank answer space. You are strictly verifying *existing* handwritten digits. If a space is blank (like the examples), it MUST remain an empty string `""`.

=== WORKSHEET CONTEXT NOTES ===
Based on the provided worksheet images and target JSON output, here is an analysis of the key factors to help write OCR instructions:

*   **Question Type:** Multi-digit vertical multiplication problems.
*   **Examples Present:** Yes. The first problem in every column (e.g., Q1, Q6, Q11, Q16 in Image 1; Q21, Q26, Q31, Q36 in Image 2) is a printed, solved example showing intermediate steps. The target JSON expects the `student_answer` for these specific examples to be left blank.
*   **Answer Format:** Handwritten numbers placed directly below the horizontal line of each vertical problem.
*   **Structural Quirks:**
    *   **Orientation:** The first image is rotated 90 degrees counter-clockwise; OCR must account for this to read the text correctly.
    *   **Numbering Flow:** The questions are numbered vertically down columns rather than horizontally across rows (e.g., the first column contains Q1 through Q5, the second contains Q6 through Q10).
    *   **Question Formatting in Output:** Although the problems are printed vertically on the page, the target JSON requires the question text to be formatted horizontally (e.g., "Q2. 333 x 91").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}