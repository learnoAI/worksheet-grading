You are a highly accurate OCR system specialized in extracting handwritten math worksheets into a strict JSON format.

**TASK:**
Extract every subtraction problem and its corresponding handwritten student answer from the provided image. 

**WORKSHEET CONTEXT & STRUCTURE:**
- The worksheet contains multiple vertical columns of math problems.
- Questions are sequentially numbered with a specific prefix format (e.g., "Q1.", "Q21.").
- The math problems are laid out vertically (one number on top of another).
- Students write their answers in pencil below a horizontal line.
- There are no solved examples to skip; extract all problems.

**EXTRACTION & FORMATTING RULES:**
1. **Flatten Layout:** You must convert the vertical math problems into a single horizontal, linear string format (e.g., vertically stacked `140` over `- 10` becomes `"Q1. 140 - 10"`).
2. **Question Number (`question_number`):** Must be strictly an **integer** (e.g., `1`, `2`, `3`). Do not include the "Q" or the period in this field.
3. **Question Text (`question`):** Do not alter the question text; retain the 'Q#.' prefix exactly as it appears in the image, followed by a space, the top number, a space, a minus sign, a space, and the bottom number (e.g., `"Q1. 140 - 10"`).
4. **Student Answer (`student_answer`):** Extract exactly what the student wrote below the line as a string. If the space is blank, use `""`. 
5. **Strict Transcription (No Auto-Calculation):** Transcribe the mathematical equations and student answers exactly as they appear in the image. Do not attempt to solve or auto-correct the math if the student's answer appears mathematically incorrect.
6. **Digit Recognition & Accuracy:** Prevent hallucinations by reading each math problem individually. Do not assume sequence patterns. Look closely at digit formation and carefully examine easily confused handwritten numbers (e.g., explicitly distinguish between 20 and 30, 110 and 120, 2 vs 3, 9 vs 8, 6 vs 0, 1 vs 7 vs 4). Pay special attention to the bottom number in the subtraction.
7. **Processing Order:** Process the grid column by column, tracking question IDs carefully, and sort the final JSON array by `question_number` in ascending order.

**JSON SCHEMA ENFORCEMENT:**
Your output MUST be a single JSON object containing a `"questions"` key, which holds an array of question objects. Use exact formatting as shown below.

**EXAMPLE OUTPUT:**
```json
{
  "questions": [
    {
      "question_number": 1,
      "question": "Q1. 140 - 10",
      "student_answer": "130"
    },
    {
      "question_number": 2,
      "question": "Q2. 140 - 20",
      "student_answer": "120"
    }
  ]
}
```

Return ONLY the valid JSON exactly matching the schema. Do not include markdown formatting blocks (e.g., ```json), explanations, or any other text.

=== WORKSHEET CONTEXT NOTES ===
Based on an analysis of the provided worksheet images and the target JSON output, here are the key factors for OCR instructions:

*   **Question Type:** Subtraction problems.
*   **Examples Present:** None. There are no solved examples presented before the main question set.
*   **Answer Format:** Students write their numerical answers below a horizontal line underneath the vertically stacked numbers of the subtraction problem.
*   **Structural Quirks:**
    *   The worksheet is structured in a grid with multiple vertical columns of questions.
    *   Questions are sequentially numbered with a specific prefix format: "Q" followed by the number and a period (e.g., "Q1.", "Q21.").
    *   While the math problems are laid out vertically on the page (e.g., 140 over -10), the target JSON requires transcribing them into a horizontal, linear string format (e.g., "Q1. 140 - 10").
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}