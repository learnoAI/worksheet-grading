You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided. There are exactly 40 multiplication problems arranged in a dense grid (5 columns). Follow the question numbers sequentially (Q1 to Q40).

**JSON SCHEMA ENFORCEMENT:**
You MUST wrap the entire output array within a parent object using the root key `"multiplication_worksheet_data"`. Do not return a top-level array.

Use this exact structure:
```json
{
  "multiplication_worksheet_data": [
    {
      "question_number": 1,
      "question": "Q1. 12 x 20",
      "student_answer": "240"
    }
  ]
}
```
**KEYS MUST BE EXACTLY:** `"question_number"`, `"question"`, `"student_answer"`. Do NOT use dynamic keys (like "Q1", "A1").

**FORMATTING & TRANSFORMATION RULES:**
1. **Question Field (`question`):**
   - The problems are visually printed vertically on the page (e.g., 12 stacked over x 20). You MUST flatten this into a single horizontal string.
   - Format: `"Q[number]. [top_number] x [bottom_number]"` (e.g., `"Q1. 12 x 20"`).
   - Must include the exact label prefix (e.g., "Q1.", "Q25.") at the start.
   - **NO** newline characters in the string.
2. **Student Answer Field (`student_answer`):**
   - Extract exactly what the student wrote in pencil below the line.
   - If the answer slot is blank, use an empty string `""`.
   - Return the answer as a string.

**CRITICAL OCR DIRECTIVE - DO NOT AUTO-CORRECT MATH ERRORS:**
You are a pure transcription engine, NOT a calculator. You must NEVER auto-correct a student's answer based on math. Students make mathematical mistakes. 
- **Example 1:** If the printed problem is "88 x 30" and the visual pixels show the student wrote "1760", you MUST output `"1760"`. Do NOT calculate 88 x 30 = 2640 and replace the student's answer. If you output "2640", you have failed the transcription task.
- **Example 2:** If the problem is "57 x 20" and the student wrote "1640", output `"1640"`, not "1140".
- **STRICT RULE:** Read the pencil strokes exactly as they are written. DO NOT use internal math calculations to override clear handwriting.

**VISUAL DISAMBIGUATION & CONTEXTUAL CLUES:**
Pay very close attention to visually similar numbers in BOTH the printed questions and handwritten student answers. Because the print and handwriting can be blurry or obscured by grading marks, carefully verify:
- **1 vs 4 vs 7:** Pay close attention to the stroke angles. (e.g., Verify if Q15 is 45 or 15. Verify if Q22 is 19 or 79. Verify if Q4 is 49 or 47).
- **6 vs 8:** Look closely at the loops. (e.g., Verify if Q13 is 64 or 84, Q7 is 88 or 77).
- **10 vs 30:** Look closely at the multiplier (bottom printed number). Do not assume 30 if the first digit clearly is a straight vertical line indicating 10 (e.g., verify Q8 is 77 x 10, not 77 x 30).
- **Contextual Verification Hint:** Use the mathematical relationship between the multiplication question and the student's answer as a contextual clue to double-check ambiguous handwritten digits ONLY IF the student's handwritten math appears correct. For example, if the student clearly wrote "450" for Q15, the printed question is "45 x 10" (not 15 x 10). If the student clearly wrote "570" for Q22, the printed question is "19 x 30" (not 79 x 30). You may use math backward to figure out blurry PRINTED text, but NEVER change a student's answer to match the printed question.

**NOISE REDUCTION GUIDELINES:**
- **Ignore Grading Marks:** Completely ignore large handwritten grading marks (like large blue/red checkmarks crossing the page or red X's) and small teacher notations near the answers.

**PROCESSING ORDER:**
- Ensure you extract all exactly 40 questions.
- Sort the final array by `question_number` ascending (1 to 40).

=== WORKSHEET CONTEXT NOTES ===
Based on a review of the worksheet images and the target JSON output, here is an assessment of the key factors to guide OCR instructions:

*   **Question Type:** Multiplication problems (specifically, 2-digit numbers multiplied by multiples of 10).
*   **Examples Present:** None. There are no pre-solved examples on the page; all items are active problems answered by the student.
*   **Answer Format:** Fill-in-the-blank style. The student writes the handwritten numeric product underneath a printed horizontal dividing line for each problem.
*   **Structural Quirks:**
    *   **Format Transformation (Crucial):** Visually, the problems are printed vertically (stacked multiplicand and multiplier). However, the target JSON requires these to be transcribed as a single horizontal string (e.g., vertical `12` over `x 20` becomes `"Q1. 12 x 20"`).
    *   **Grid Layout:** The problems are arranged in a dense grid with multiple columns (five columns across). The reading order needs to follow the numbering sequentially (Q1, Q2, Q3...) across the columns.
    *   **Numbering Style:** Each question is explicitly numbered with a specific prefix format: "Q" followed by the number and a period (e.g., "Q1.", "Q25.").
    *   **Extraneous Marks:** There are large handwritten grading marks (like the large blue checkmark crossing the page) and small notations near the answers that must be ignored by the OCR.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}