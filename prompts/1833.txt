You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every subtraction question and its corresponding handwritten student answer from the provided worksheet image. 

**JSON SCHEMA ENFORCEMENT:**
You MUST wrap your output in a root object with the key `"submission"`. The value must be an array of objects.
Follow this exact structure:
```json
{
  "submission": [
    {
      "question_number": 1,
      "question": "Q1. 266 - 70",
      "student_answer": "196"
    }
  ]
}
```

**DO NOT** use dynamic keys (like "Q1", "A1").
**DO NOT** omit the `"submission"` root wrapper.

**FIELD FORMATTING RULES:**
1.  **`question_number` (Integer):**
    - Extract ONLY the numeric value of the question sequence (e.g., for "Q1.", output the integer `1`). 
    - **DO NOT** output strings like "Q1.".
2.  **`question` (String):**
    - MUST include the full question label prefix exactly as written (e.g., "Q1.", "Q2.").
    - MUST flatten the vertical math problem into a single horizontal equation separated by a space, the minus sign, and a space (e.g., `"Q1. 266 - 70"`).
    - **NO** newline characters.
3.  **`student_answer` (String):**
    - Extract exactly the numeric answer the student wrote in pencil beneath the solid line.
    - If the answer slot is blank, use an empty string `""`.
    - Return the answer as a string (e.g., `"196"`).

**WORKSHEET CONTEXT & PROCESSING ORDER:**
- **Layout:** The questions are arranged in a 4-column grid format.
- **Sorting:** Ensure the final array is sorted strictly by `question_number` in ascending order (from 1 up to the final question, e.g., 40).
- **Exclusions:** Ignore page headers (Name, Token Number, Marks, "Subtraction - 1833"), instructional text ("Subtract the following", "घटाओ"), and logos at the bottom.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation beneath the solid line of each problem.
- Distinguish carefully between **9** vs **8**, **6** vs **0**, and **1** vs **7** vs **4**.
- If a handwritten digit is messy, select the most likely digit based on visual stroke evidence.

=== WORKSHEET CONTEXT NOTES ===
*   **Question Type:** Subtraction (vertical format).
*   **Examples Present:** None.
*   **Answer Format:** Written numerically beneath the problem, below a solid line.
*   **Structural Quirks:** The problems are arranged in a grid-like layout with four columns. Each problem is numbered with a 'Q' prefix (e.g., 'Q1.', 'Q2.'). The instruction "Subtract the following" and its Hindi translation "घटाओ" appear at the top. The top right corner includes headers for 'Name:', 'Token Number:', and 'Marks:', alongside a title 'Subtraction - 1833'. At the bottom, there's a logo for "Saarthi EDUCATION".
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}