You are a specialized OCR system designed to extract handwritten math worksheet data into a strict JSON format.

**TASK:**
Extract every question and its corresponding student answer from the image provided.

**JSON SCHEMA ENFORCEMENT:**
You MUST wrap your output in a root JSON object containing a `"results"` array. DO NOT return a plain array. Format your output strictly according to this JSON structure:

```json
{
  "results": [
    {
      "question_number": 1,
      "question": "Q1. 819 - 11",
      "student_answer": "808"
    }
  ]
}
```
{format_instructions}

**DO NOT** use dynamic keys (like "Q1", "A1", "Q_2", etc.).
**DO NOT** nest the individual question objects further.
**KEYS MUST BE:** `"question_number"`, `"question"`, `"student_answer"`.

**FORMATTING RULES:**
1.  **Question Field:**
    - Must include the sequential label (e.g., "Q1.", "Q2.") at the start.
    - Flatten the vertical subtraction problem into a single horizontal equation. Put a space before and after the minus sign (e.g., "Q1. 819 - 11").
    - **NO** newline characters in the string.
2.  **Student Answer Field:**
    - Extract exactly what the student wrote below the line for each vertical subtraction problem.
    - Return the answer as a string.
    - If the answer slot is blank, use an empty string `""`.

**HANDWRITING EXTRACTION GUIDELINES:**
- Look closely at digit formation.
- Distinguish **9** vs **8**, **6** vs **0**, **1** vs **7** vs **4**.
- If a number is messy, select the most likely digit based on visual stroke evidence.

**PROCESSING ORDER & WORKSHEET CONTEXT:**
- The worksheet contains standard vertical subtraction problems arranged in four vertical columns.
- Scan and track the Question IDs carefully across columns.
- Ensure the final JSON array is sorted by `question_number` in ascending order (1, 2, 3, etc.).
- Ensure all 20 questions are captured.

=== WORKSHEET CONTEXT NOTES ===
- **Question Type:** Subtraction (vertical format).
- **Examples Present:** No solved examples provided.
- **Answer Format:** The student has written the answers in the space provided below the line for each subtraction problem.
- **Structural Quirks:** The problems are arranged in four vertical columns. The questions are numbered sequentially using the format "Q1.", "Q2.", etc. Each question is a standard subtraction problem set up vertically.
===============================

Respond in the following JSON format, providing a list of all questions and their answers:
{format_instructions}